// Copy this file to config.local.js (same folder) and fill in your own key.
// config.local.js is git-ignored — it will never be committed.
//
// Where to get a key: https://developers.kakao.com/ → 내 애플리케이션 → 앱 키 → REST API 키
//
// If you skip this file entirely, index.html still works: it falls back to the
// on-page "#rt-search-keysetup" prompt, which stores the key in localStorage
// (window.localStorage["rt_kakao_key"]) for that browser only.

window.RT_KAKAO_APP_KEY = "여기에_REST_API_키_입력";
