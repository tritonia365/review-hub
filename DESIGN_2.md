# DESIGN.md — 리얼 테이블 (Real Table)

| 항목 | 내용 |
|---|---|
| **문서 버전** | v1.0 |
| **작성일** | 2026-08-14 |
| **연관 문서** | [PRD.md](./PRD.md) |
| **적용 범위** | 랜딩페이지(Phase 0)부터 전 서비스 |

> ⚠️ 이 문서는 PRD §3(브랜드 & 디자인 시스템)을 **대체**한다. 토큰이 충돌하면 DESIGN.md가 우선한다. 변경 내역은 §12 참조.

---

## 목차

- [1. 디자인 방향 요약](#1-디자인-방향-요약)
- [2. 무드](#2-무드)
- [3. 컬러](#3-컬러)
- [4. 타이포그래피](#4-타이포그래피)
- [5. 레이아웃 · 간격](#5-레이아웃--간격)
- [6. 사진 처리](#6-사진-처리)
- [7. 시그니처 — 접시 모티프](#7-시그니처--접시-모티프)
- [8. 컴포넌트](#8-컴포넌트)
- [9. 모션](#9-모션)
- [10. 보이스 & 톤](#10-보이스--톤)
- [11. 레퍼런스 분해](#11-레퍼런스-분해)
- [12. PRD 대비 변경 사항](#12-prd-대비-변경-사항)
- [13. Do / Don't](#13-do--dont)
- [14. 구현 토큰](#14-구현-토큰)

---

# 1. 디자인 방향 요약

> **"흰 접시 위의 한 줄."**
> 아이보리 종이 위에 명조로 조판된 기록. 노랑은 신호로만 쓰고, 사진과 여백이 말하게 한다.

| 축 | 결정 |
|---|---|
| 무드 | **정제된 고급스러움** — 미슐랭·캐치테이블형. 여백 많고 절제된 |
| 베이스 | **따뜻한 아이보리 `#FDFBF4`** — 종이 질감 |
| 타이포 | **제목 국문 명조 + 본문 Pretendard** 듀얼 시스템 |
| 레퍼런스 | **미슐랭 가이드**(훈장·명조 편집) + **에어비앤비**(사진 그리드·필터·신뢰 장치) |
| 시그니처 | **접시 모티프** — 핵심 정보는 모두 원형으로 (§7) |
| 제외 | ❌ 배달의민족형 노랑 운용 — 무드가 정반대 |

## 1.1 디자인 원칙

1. **사진이 주인공, UI는 접시다.** 배경·보더·그림자는 음식 사진보다 절대 튀지 않는다.
2. **노랑은 신호이지 배경이 아니다.** 화면 면적의 5%를 넘기지 않는다 (§3.3).
3. **여백이 고급감을 만든다.** 요소를 더 넣기 전에 간격을 먼저 늘린다.
4. **명조는 제목에만.** 본문에 명조를 쓰는 순간 가독성이 무너진다 (§4.4).
5. **장식하지 않고 구조화한다.** 선·라벨·번호는 정보가 있을 때만 쓴다.

---

# 2. 무드

## 2.1 무드 정의

**기념일에 실패하면 안 되는 사람**이 쓰는 서비스다. 팔아야 할 감정은 설렘이 아니라 **확신**이다.

| 지향 | 회피 |
|---|---|
| 조용하고 단단한 | 들뜨고 시끄러운 |
| 편집된 (매거진) | 나열된 (카탈로그) |
| 기록 · 아카이브 | 광고 · 프로모션 |
| 여백으로 만드는 밀도 | 정보로 채우는 밀도 |
| 절제된 확신 | 과장된 추천 |

## 2.2 한 줄 무드보드

> 하얀 리넨 테이블보, 접힌 종이 메뉴판, 잉크로 눌러쓴 한 줄,
> 유리문에 붙은 작은 금색 씰(seal), 그리고 조명 아래 놓인 접시.

## 2.3 ⚠️ 이 무드의 최대 위험 — 노랑

`#FFE94F`는 본래 캐주얼한 색이다. 절제 없이 쓰면 **3초 만에 배달앱 톤**으로 떨어진다.

**해법: 노랑의 역할을 좁히고, 골드와 분리한다.**

| 색 | 역할 | 쓰는 곳 |
|---|---|---|
| 🟡 **브랜드 노랑** | **행동(Action)** | CTA 버튼, 활성 필터, 포커스, 검색어 하이라이트 |
| 🥇 **골드** | **성취(Achievement)** | 훈장, 빌보드 1위, 명예의 테이블 |

노랑은 "여기를 누르세요", 골드는 "이 가게가 해냈습니다". **두 색이 한 화면에서 경쟁하지 않도록** 골드는 훈장 영역 밖으로 나가지 않는다.

---

# 3. 컬러

## 3.1 팔레트

```
Base        Ink          Brand        Gold         Semantic
#FDFBF4     #1C1B17      #FFE94F      #C9A227      #C0392B
아이보리     잉크          노랑          골드          레드
```

## 3.2 토큰

```css
:root {
  /* ── Base — 종이 ─────────────────────── */
  --rt-paper:        #FDFBF4;  /* 기본 배경. 아이보리 */
  --rt-paper-deep:   #F7F3E8;  /* 섹션 구분 배경, 비활성 영역 */
  --rt-paper-pure:   #FFFFFF;  /* 카드·모달 면 (종이 위의 종이) */

  /* ── Ink — 텍스트 ────────────────────── */
  --rt-ink:          #1C1B17;  /* 제목, 본문. 순흑 아님 — 종이와 어울리는 웜 블랙 */
  --rt-ink-70:       #55534B;  /* 보조 텍스트 */
  --rt-ink-50:       #85827A;  /* 캡션, 메타 */
  --rt-ink-30:       #B5B2A8;  /* 플레이스홀더, 비활성 */
  --rt-line:         #E5E0D3;  /* 구분선, 카드 보더. 아이보리와 같은 계열 */
  --rt-line-soft:    #F0ECE0;  /* 미세 구분선 */

  /* ── Brand — 행동 ────────────────────── */
  --rt-yellow:       #FFE94F;  /* CTA, 활성 상태 */
  --rt-yellow-deep:  #EBD33A;  /* hover / pressed */
  --rt-yellow-wash:  #FFF8CC;  /* 검색어 하이라이트, 선택 배경 */

  /* ── Gold — 성취 (훈장 전용) ──────────── */
  --rt-gold:         #C9A227;
  --rt-silver:       #9A9A94;
  --rt-bronze:       #A9713F;

  /* ── Semantic ───────────────────────── */
  --rt-red:          #C0392B;  /* 신고, 오류, 제재 */
  --rt-red-wash:     #FBEFED;
  --rt-green:        #2F7D4F;  /* 인증 완료 */
  --rt-green-wash:   #EDF5F0;
}
```

### 색 선택 근거

| 토큰 | 왜 이 값인가 |
|---|---|
| `--rt-paper #FDFBF4` | 순백보다 채도가 낮아 **음식 사진의 붉은·주황 계열이 더 선명**해진다. 노랑과 같은 웜 계열이라 CTA가 이물감 없이 얹힌다 |
| `--rt-ink #1C1B17` | 순흑 `#000`은 아이보리 위에서 차갑게 뜬다. 웜 블랙이 종이 질감과 맞물린다 |
| `--rt-line #E5E0D3` | 회색 보더는 아이보리 위에서 지저분해 보인다. **같은 웜 계열의 한 톤 어두운 선** |
| `--rt-gold #C9A227` | 브랜드 노랑보다 **채도를 낮추고 명도를 내려** 금속 질감을 낸다. 노랑과 충돌하지 않음 |
| `--rt-red #C0392B` | 형광 계열 빨강은 아이보리 위에서 튄다. 벽돌빛 레드 |

## 3.3 ⚠️ 노랑 면적 규칙

| 규칙 | 내용 |
|---|---|
| **5% 상한** | 한 화면에서 노랑이 차지하는 면적은 5%를 넘지 않는다 |
| **대면적 금지** | 헤더 바, 섹션 전체 배경, 히어로 배경에 노랑 금지 |
| **1화면 1주역** | 화면당 노랑 CTA는 **하나**. 두 개면 위계가 사라진다 |
| **텍스트 금지** | 아이보리 위 노란 텍스트 = 대비 1.2:1. **절대 금지** |
| **노랑 위 텍스트** | 반드시 `--rt-ink` (대비 약 14:1) |
| **사진 위 금지** | 음식 사진 위에 노랑을 얹지 않는다. 음식 색과 충돌 |

### 색상 대비 검증

| 조합 | 대비 | 판정 |
|---|---|---|
| `--rt-ink` on `--rt-paper` | 14.8:1 | ✅ AAA |
| `--rt-ink-70` on `--rt-paper` | 7.4:1 | ✅ AAA |
| `--rt-ink-50` on `--rt-paper` | 3.9:1 | ⚠️ 캡션(13px+)만, 본문 금지 |
| `--rt-ink` on `--rt-yellow` | 14.1:1 | ✅ AAA |
| `--rt-yellow` on `--rt-paper` | 1.2:1 | ❌ **텍스트 사용 금지** |
| `--rt-gold` on `--rt-paper` | 3.2:1 | ⚠️ 18px+ 굵은 글자만 |

⚠️ **색 단독으로 정보를 전달하지 않는다.** 별점·상태·오류는 항상 텍스트나 아이콘을 병기한다.

## 3.4 훈장 컬러 (§PRD 14.5)

| 훈장 | 메인 | 그라디언트 | 테두리 |
|---|---|---|---|
| 🥇 금 | `#C9A227` | `#E3C766` → `#A8841C` | `#8C6D14` |
| 🥈 은 | `#9A9A94` | `#C8C8C2` → `#7E7E78` | `#6B6B66` |
| 🥉 동 | `#A9713F` | `#C99260` → `#8A5A2F` | `#734B27` |

금속 질감은 **2스톱 선형 그라디언트 + 1px 어두운 테두리**로만 표현한다. 광택 하이라이트·베벨은 쓰지 않는다 (2000년대 웹 느낌).

---

# 4. 타이포그래피

## 4.1 듀얼 서체 시스템

| 역할 | 서체 | 라이선스 | 용도 |
|---|---|---|---|
| **Display** | **본명조 (Noto Serif KR)** | OFL 1.1 | 제목, 히어로, 섹션 헤드, 인용 |
| **Body** | **Pretendard Variable** | OFL 1.1 | 본문, UI 전체, 라벨, 버튼 |
| **Numeric** | Pretendard (`tabular-nums`) | OFL 1.1 | 평점, 통계, 순위, 가격 |

```css
--rt-font-display: "Noto Serif KR", "본명조", "Nanum Myeongjo",
                   "Apple SD Gothic Neo", serif;
--rt-font-body:    "Pretendard Variable", Pretendard, -apple-system,
                   BlinkMacSystemFont, system-ui, "Apple SD Gothic Neo",
                   "Noto Sans KR", sans-serif;
```

**왜 이 조합인가**
명조는 획의 굵기 차이와 삐침이 있어 **"편집된 것"** 이라는 인상을 준다. 미슐랭 가이드와 잡지가 세리프를 쓰는 이유다. 반면 리뷰 본문은 15px 안팎으로 길게 이어지므로, 화면에서 획이 뭉치는 명조는 부적합하다. **제목은 명조로 권위를, 본문은 산세리프로 가독성을** 가져간다.

**대안**: 히어로 한정으로 **고운바탕(Gowun Batang)** 사용 가능. 더 가늘고 우아하지만 400/700 두 굵기뿐이라 스케일 전반에는 부적합.

## 4.2 타입 스케일

| 스타일 | 서체 | 모바일 / 데스크톱 | Weight | 자간 | 행간 |
|---|---|---|---|---|---|
| **Display** | 명조 | 30 / 52 | 600 | -0.02em | 1.35 |
| **H1** | 명조 | 24 / 36 | 600 | -0.02em | 1.4 |
| **H2** | 명조 | 20 / 26 | 600 | -0.01em | 1.45 |
| **H3** | Pretendard | 17 / 19 | 600 | -0.01em | 1.5 |
| **Quote** | 명조 | 17 / 20 | 400 | 0 | 1.7 |
| **Body** | Pretendard | 15 / 16 | 400 | 0 | **1.75** |
| **Body Strong** | Pretendard | 15 / 16 | 600 | 0 | 1.75 |
| **Label** | Pretendard | 14 / 14 | 500 | 0 | 1.4 |
| **Caption** | Pretendard | 13 / 13 | 400 | 0.01em | 1.5 |
| **Eyebrow** | Pretendard | 12 / 12 | 600 | **0.12em** | 1.4 |
| **Numeric L** | Pretendard | 28 / 34 | 700 | -0.02em | 1 |

### 리뷰 본문 특별 규정

리뷰는 이 서비스에서 **가장 오래 읽히는 텍스트**다.

```css
.review-body {
  font-size: 15px;          /* 모바일 */
  line-height: 1.75;        /* 일반 UI(1.6)보다 넉넉하게 */
  letter-spacing: 0;
  max-width: 38em;          /* 한 줄 약 33~36자 — 한글 최적 가독 폭 */
  word-break: keep-all;     /* ⚠️ 한글 단어 중간 끊김 방지 */
  overflow-wrap: break-word;
  color: var(--rt-ink);
}
```

⚠️ **`word-break: keep-all` 은 한글 서비스에서 필수다.** 기본값이면 "기념일에"가 "기념일/에"로 쪼개져 읽기 리듬이 깨진다.

## 4.3 명조 사용 규칙

| ✅ 명조를 쓴다 | ❌ 명조를 쓰지 않는다 |
|---|---|
| 히어로 헤드라인 | **본문·리뷰 텍스트** |
| 섹션 제목 (H1·H2) | 버튼 라벨 |
| 식당명 (상세 페이지 상단) | 폼 라벨·인풋 |
| 대표 리뷰 인용문 | 숫자·평점·통계 |
| 훈장 수여 문구 | 필터 칩·태그 |
| 빌보드 순위 타이틀 | 14px 이하 모든 텍스트 |

⚠️ **명조 최소 크기 18px.** 그 이하에서는 획이 뭉쳐 저해상도 화면에서 읽기 어렵다.
⚠️ **명조 Weight는 400·600·700만.** 300 이하는 아이보리 배경에서 사라진다.

## 4.4 숫자 표기

```css
.numeric {
  font-family: var(--rt-font-body);
  font-variant-numeric: tabular-nums;
  font-feature-settings: "tnum" 1;
}
```

평점·순위·리뷰 수는 **자릿수가 정렬**되어야 목록에서 비교가 된다. 평점은 항상 소수점 1자리 고정 (`4.0` ✅ / `4` ❌).

## 4.5 폰트 로딩

```
Noto Serif KR   → 600 한 굵기만 subset 로드 (제목 전용, 글자 수 적음)
Pretendard      → Variable, KR subset, preload
```

- `font-display: swap`
- ⚠️ 명조 폴백을 `serif`로만 두면 Windows에서 바탕체가 나와 인상이 완전히 달라진다. `Nanum Myeongjo` 웹폰트를 2차 폴백으로 둔다
- 명조는 **히어로에서만 preload**, 나머지는 lazy

---

# 5. 레이아웃 · 간격

## 5.1 간격 스케일

```css
--rt-space-1:  4px;   --rt-space-2:  8px;   --rt-space-3: 12px;
--rt-space-4: 16px;   --rt-space-5: 24px;   --rt-space-6: 32px;
--rt-space-7: 48px;   --rt-space-8: 64px;   --rt-space-9: 96px;
--rt-space-10: 128px;
```

## 5.2 여백 원칙

**고급감은 요소가 아니라 간격에서 나온다.**

| 위치 | 모바일 | 데스크톱 |
|---|---|---|
| 섹션 간 수직 여백 | 64px | **128px** |
| 섹션 내 블록 간 | 32px | 48px |
| 화면 좌우 여백 | 20px | 40px (최대폭 밖) |
| 카드 내부 패딩 | 20px | 24px |
| 카드 간 간격 | 16px | 24px |

⚠️ **요소를 추가하기 전에 여백을 먼저 늘려본다.** 이 무드에서 밀도는 적이다.

## 5.3 그리드

| 브레이크포인트 | 컬럼 | 최대폭 | 리스트 열 |
|---|---|---|---|
| 320~374 | 4 | — | 1 |
| 375~767 | 4 | — | 1 |
| 768~1023 | 8 | — | 2 |
| 1024~1439 | 12 | 1120px | 2 + 필터 사이드바 |
| 1440~ | 12 | **1200px** | 3 |

**본문 최적 폭**: 리뷰 상세·정책 문서 등 긴 글은 `max-width: 680px` 중앙 정렬.

## 5.4 형태

```css
--rt-radius-sm:   6px;   /* 태그, 배지 */
--rt-radius-md:  10px;   /* 버튼, 인풋 */
--rt-radius-lg:  14px;   /* 카드, 이미지 */
--rt-radius-xl:  20px;   /* 모달, 바텀시트 */
--rt-radius-full: 999px; /* 칩, 아바타, 훈장 */
```

⚠️ **라운드는 과하지 않게.** 20px 이상 라운드는 캐주얼해 보인다. 미슐랭 톤에서는 각이 살아 있어야 한다.

## 5.5 그림자 · 보더

```css
--rt-shadow-sm: 0 1px 2px rgba(28,27,23,0.04);
--rt-shadow-md: 0 4px 16px rgba(28,27,23,0.07);
--rt-shadow-lg: 0 12px 40px rgba(28,27,23,0.10);
```

⚠️ **기본은 그림자가 아니라 보더 1px `--rt-line`.** 아이보리 배경에서 그림자를 남발하면 종이 질감이 탁해진다. 그림자는 **떠 있어야 하는 것**(모달, 바텀시트, 호버 상태)에만.

---

# 6. 사진 처리

**사진이 주인공이다.** 아래 규칙은 사진을 돋보이게 하기 위한 제약이다.

## 6.1 비율 · 형태

| 용도 | 비율 | radius |
|---|---|---|
| 리스트 카드 썸네일 | 4:3 | 상단만 14px |
| 식당 상세 히어로 | 16:9 (모바일 4:3) | 0 (풀블리드) |
| 갤러리 그리드 | 1:1 | 10px |
| 리뷰 첨부 사진 | 원본 비율 유지, 최대 높이 400px | 10px |
| 아바타 | 1:1 | full |

## 6.2 규칙

| 규칙 | 이유 |
|---|---|
| **사진 위 텍스트 금지** | 음식 사진 위 텍스트는 가독성도 식욕도 둘 다 해친다. 텍스트는 사진 **아래** |
| **필터·틴트 금지** | 실제 색을 왜곡하면 "진짜 후기"가 아니게 된다 |
| **오버레이 최소** | 필요 시 하단 `linear-gradient(transparent, rgba(28,27,23,0.5))` 만 |
| **보더 없음** | 사진에 보더를 두르지 않는다. 아이보리 배경과 자연 분리 |
| **로딩 플레이스홀더** | `--rt-paper-deep` 면 + 중앙에 식당명 이니셜 (명조) |
| **사진 없는 리뷰** | 텍스트만으로 정상 카드. 빈 이미지 영역을 남기지 않는다 |

## 6.3 `가게 제공` 라벨 (PRD §6.0)

사장님 홍보 사진에는 **상시 노출** 라벨을 붙인다.

```
좌상단 8px 오프셋
배경 rgba(28,27,23,0.72) / 텍스트 --rt-paper / 11px 500
radius 4px / padding 3px 8px
텍스트: "가게 제공"
```

⚠️ 호버·클릭으로만 보이게 하면 안 된다. **상시 보여야 기만 표시가 아니다.**

## 6.4 갤러리 (에어비앤비 문법)

- 데스크톱: 대표 1장(좌 50%) + 4장 그리드(우 50%), 4px 간격, 바깥 모서리만 라운드
- 모바일: 가로 스와이프 캐러셀 + 하단 `3 / 12` 인디케이터
- 우하단 `사진 모두 보기` 버튼 → 라이트박스
- **탭 분리**: `손님 사진` / `가게 사진` (PRD §6.0) — 기본 탭은 **손님 사진**

---

# 7. 시그니처 — 접시 모티프

> 이 서비스가 기억될 단 하나의 시각 장치.

레스토랑의 세계에서 가장 근본적인 형태는 **접시(원형)** 다. 리얼 테이블의 핵심 정보는 전부 원으로 표현한다.

| 요소 | 원형 표현 |
|---|---|
| **평점** | 원형 링 게이지 — 링이 채워진 정도가 평점, 중앙에 숫자 |
| **별점 분포** | 도넛차트 (PRD §11.2) — 이미 접시 형태 |
| **훈장** | 원형 씰(seal) — 유리문에 붙이는 스티커 그 자체 |
| **아바타** | 원형 |
| **로딩** | 원형 회전이 아니라 **링이 차오르는** 형태 |

## 7.1 평점 링

```
     ╭───────╮
    ╱   4.6   ╲      바깥 링: 평점 비율만큼 --rt-ink 로 채움
   │  ★ 42건  │      나머지: --rt-line
    ╲         ╱      중앙 숫자: Pretendard 700, tabular-nums
     ╰───────╯       지름: 모바일 56px / 데스크톱 72px
```

⚠️ 링 색은 **노랑이 아니라 잉크**다. 노랑을 여기 쓰면 §3.3의 5% 규칙이 즉시 깨진다.

## 7.2 훈장 씰

```
   ╭─────────╮
  │  ╭─────╮  │     외곽: 톱니 테두리 (실링 왁스 느낌)
  │  │ 金  │  │     내부: 금속 그라디언트
  │  ╰─────╯  │     중앙: 등급 문자 (명조 700)
  │ 2026. 07  │     하단: 획득 연월 (Pretendard 11px)
   ╰─────────╯
```

- 지름: 카드 40px / 상세 72px / 키트용 원본 512px
- ⚠️ **획득 연월 필수 표기** (PRD §14.5)
- 명예의 테이블(금 3회)만 외곽에 얇은 월계 라인 추가

## 7.3 절제 규칙

⚠️ **원형 모티프를 남발하지 않는다.** 버튼·카드·인풋은 사각이다. 원은 **정보의 결정체**에만 쓴다. 모든 게 둥글면 시그니처가 아니라 그냥 스타일이 된다.

---

# 8. 컴포넌트

## 8.1 버튼

| 종류 | 배경 | 텍스트 | 보더 | 용도 |
|---|---|---|---|---|
| **Primary** | `--rt-yellow` | `--rt-ink` 600 | 없음 | 화면당 **1개** |
| **Secondary** | `--rt-paper-pure` | `--rt-ink` 600 | 1.5px `--rt-ink` | 보조 행동 |
| **Ghost** | 투명 | `--rt-ink-70` 500 | 없음 | 3순위 |
| **Danger** | 투명 | `--rt-red` 600 | 1px `--rt-red` | 신고, 삭제 |

```
높이   48px(모바일) / 44px(데스크톱)
radius 10px
패딩   좌우 20px
서체   Pretendard 15px 600  ⚠️ 명조 금지
hover  Primary: --rt-yellow-deep + translateY(-1px)
active scale(0.98)
focus  outline 2px --rt-ink, offset 2px
```

## 8.2 리뷰 카드

```
┌────────────────────────────┐
│ [ 사진 4:3, 상단 radius 14 ]│  ← 사진 없으면 이 영역 자체가 없음
│                        📷 5│
├────────────────────────────┤
│ 테이블 포 투          ╭───╮ │  ← 식당명 Pretendard 600 17px
│ 강남구 신사동         │4.6│ │     평점 링 (§7.1)
│                      ╰───╯ │
│ 〔기념일〕〔조용함〕        │  ← 칩
│                            │
│ 3주년 기념일에 방문했어요.  │  ← 본문 2줄 말줄임
│ 룸으로 안내받아서 대화하...│     line-height 1.75
│                            │
│ 조용한테이블 · 2일 전   🥇 │  ← 메타 + 훈장(있으면)
└────────────────────────────┘
배경 --rt-paper-pure / 보더 1px --rt-line / radius 14px
hover translateY(-2px) + shadow-md, 200ms
```

⚠️ 카드 배경은 **순백**, 페이지 배경은 **아이보리**. 이 미세한 명도 차가 종이 위의 종이 느낌을 만든다.

## 8.3 필터 칩 (에어비앤비 문법)

```
비활성  배경 --rt-paper-pure / 보더 1px --rt-line / 텍스트 --rt-ink-70
hover   보더 --rt-ink
활성    배경 --rt-yellow / 보더 없음 / 텍스트 --rt-ink 600 / 좌측 ✓
높이 36px / radius full / 패딩 좌우 14px / 14px 500
```

활성 칩이 노랑을 쓰는 유일한 반복 요소다. **동시 활성 칩이 5개를 넘으면** 노랑 면적이 규칙을 넘기므로, 6개 이상은 `필터 6` 배지로 접는다.

## 8.4 별점

```
★★★★☆ 4.6    별: --rt-ink (노랑 아님)
              빈 별: --rt-line
              숫자: Pretendard 600 tabular-nums, 반드시 병기
```

⚠️ 별을 노랑으로 칠하면 모든 카드에 노랑이 깔려 §3.3이 붕괴한다. **별은 잉크색이다.**

## 8.5 인풋

```
높이 48px / 배경 --rt-paper-pure / 보더 1.5px --rt-line / radius 10px
placeholder --rt-ink-30
focus 보더 --rt-ink + box-shadow 0 0 0 3px var(--rt-yellow-wash)
error 보더 --rt-red + 하단 메시지 (색 단독 금지)
⚠️ font-size 16px 이상 (iOS 자동 확대 방지)
```

## 8.6 탭

```
비활성 --rt-ink-50 / 활성 --rt-ink 600
활성 표시: 하단 2px --rt-ink 언더라인 (노랑 아님)
전환: 언더라인 좌우 슬라이드 250ms
```

## 8.7 빈 상태

| 상황 | 카피 | 시각 |
|---|---|---|
| 검색 결과 없음 | `조건에 맞는 곳이 없어요` + `필터 줄이기` 버튼 | 빈 접시 라인 일러스트 |
| 리뷰 없음 | `아직 첫 리뷰를 기다리고 있어요` | 접시 + 점선 |
| 훈장 없음 | `아직 받은 훈장이 없어요` | 흐린 씰 |

빈 화면은 분위기가 아니라 **다음 행동을 제안하는 자리**다. 반드시 버튼을 함께 둔다.

---

# 9. 모션

## 9.1 토큰

```css
--rt-dur-fast:   150ms;   /* 색 전환, 호버 */
--rt-dur-base:   250ms;   /* 대부분 */
--rt-dur-slow:   450ms;   /* 스크롤 리빌 */
--rt-dur-draw:   800ms;   /* 링·도넛 드로잉 */

--rt-ease-out:  cubic-bezier(0.22, 1, 0.36, 1);
--rt-ease-soft: cubic-bezier(0.4, 0, 0.2, 1);
```

## 9.2 오케스트레이션

**흩뿌린 효과보다 한 번의 연출.** 이 무드에서 모션은 조용해야 한다.

| 효과 | 사양 |
|---|---|
| 섹션 리빌 | `opacity 0→1` + `translateY(20px→0)`, 450ms, 1회만 |
| 카드 stagger | 60ms 간격, **최대 6개** 이후 동시 |
| 평점 링 드로잉 | 뷰포트 진입 시 0→값, 800ms `--rt-ease-out` |
| 훈장 등장 | `scale(0.9→1)` + `opacity`, 350ms — **회전·반짝임 금지** |
| 카드 hover | `translateY(-2px)`, 200ms |
| 사진 hover | 내부 이미지 `scale(1.03)`, 400ms |
| 페이지 전환 | 콘텐츠 fade 150ms |

## 9.3 필수 제약

```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: .01ms !important;
    transition-duration: .01ms !important;
    scroll-behavior: auto !important;
  }
}
@media (hover: hover) and (pointer: fine) {
  /* 호버 효과는 여기 안에서만 */
}
```

⚠️ `transform`·`opacity`만 애니메이션한다. `width`·`height`·`top`은 리플로우를 유발한다.

---

# 10. 보이스 & 톤

## 10.1 원칙

| 지향 | 회피 |
|---|---|
| 담담한 존댓말 | 과장된 감탄 |
| 구체적 사실 | 형용사 나열 |
| 사용자의 언어 | 시스템의 언어 |
| 짧은 문장 | 설명형 장문 |

## 10.2 UI 카피 규칙

- **문장부호 절제**: 느낌표를 쓰지 않는다. `저장했어요` ✅ / `저장했어요!` ❌
- **동사 일관성**: 버튼이 `저장`이면 토스트는 `저장했어요`
- **사용자 언어**: `리뷰 등록` ❌ → `후기 남기기` ✅
- **오류는 사과하지 않고 해결책을 준다**: `사진(JPG·PNG·WebP)만 올릴 수 있어요`
- **숫자는 정확하게**: `많은 후기` ❌ → `후기 42개` ✅

## 10.3 예시

| 상황 | ❌ | ✅ |
|---|---|---|
| CTA | 지금 바로 시작하기! | 사전 등록하고 먼저 받아보기 |
| 빈 결과 | 앗! 결과가 없네요 😢 | 조건에 맞는 곳이 없어요 |
| 훈장 | 축하합니다!! 금메달 획득!! | 2026년 7월, 이달의 베스트 식당 |
| 신뢰 문구 | 100% 진짜 후기 보장! | 광고를 팔지 않습니다 |
| 로딩 | 잠시만 기다려주세요... | (스켈레톤, 문구 없음) |

---

# 11. 레퍼런스 분해

## 11.1 미슐랭 가이드 — 가져올 것

| 요소 | 적용 |
|---|---|
| **성취의 시각화** | 별 → 훈장 씰 (§7.2). 작고, 금속이고, 획득 시점이 박혀 있다 |
| **명조 조판** | 제목에 세리프를 써서 "심사된 기록"의 인상을 만든다 |
| **극단적 절제** | 색을 거의 쓰지 않는다. 정보는 활자와 여백으로만 |
| **권위의 언어** | 감탄하지 않고 선언한다 (§10.3) |

**가져오지 않을 것**: 폐쇄적 심사 문법. 리얼 테이블의 훈장은 익명 손님들의 리뷰 데이터로 결정된다 (PRD §14.5). 시각 언어는 빌리되, **선정 주체가 다르다는 점을 UI에서 드러낸다** — 훈장 옆에 항상 `리뷰 N건 기준` 표기.

## 11.2 에어비앤비 — 가져올 것

| 요소 | 적용 |
|---|---|
| **사진 그리드** | 1+4 갤러리, 4px 간격, 바깥 모서리만 라운드 (§6.4) |
| **필터 UX** | 모바일 바텀시트, 활성 개수 배지, 즉시 결과 수 표시 |
| **신뢰 장치** | 배지·인증 표시를 정보 위계 상단에 배치 |
| **카드 위계** | 사진 → 이름 → 위치 → 평점 → 메타. 시선 순서가 고정 |
| **여백** | 카드 간 24px, 섹션 간 128px |

**가져오지 않을 것**: 채도 높은 로즈/코랄 계열 액센트. 우리 액센트는 노랑 하나뿐이다.

## 11.3 캐치테이블 — 부분 참고

정보 위계와 예약 흐름의 긴장감은 참고하되, **채택 우선순위는 아니다.**

## 11.4 ⚠️ 배달의민족 — 명시적 제외

| 배민의 방식 | 리얼 테이블 |
|---|---|
| 노랑을 대면적 배경으로 | 노랑은 5% 이하 액센트 (§3.3) |
| 개성 강한 전용 서체(한나·주아) | 본명조 + Pretendard |
| 유머러스한 카피 | 담담한 존댓말 (§10) |
| 밀도 높은 정보 나열 | 여백 중심 편집 |

⚠️ **노랑을 쓴다는 이유로 배민 톤을 참조하면 무드가 즉시 붕괴한다.** 같은 색을 완전히 다르게 쓰는 것이 이 디자인의 과제다.

---

# 12. PRD 대비 변경 사항

DESIGN.md가 PRD §3을 대체한다. 주요 델타:

| 항목 | PRD v1.0 | DESIGN v1.0 | 사유 |
|---|---|---|---|
| 기본 배경 | `#FFFFFF` 순백 | **`#FDFBF4` 아이보리** | 종이 질감, 음식 사진 발색 |
| 텍스트 | `#1A1A17` | **`#1C1B17`** | 아이보리와 맞춘 웜 블랙 |
| 보더 | `#E7E7E2` 회색 | **`#E5E0D3` 웜 라인** | 아이보리 위 회색은 탁함 |
| 제목 서체 | Pretendard 800 | **본명조(Noto Serif KR) 600** | 미슐랭 무드 |
| 훈장 금색 | `#D4AF37` | **`#C9A227`** | 브랜드 노랑과의 충돌 완화 |
| 별점 색 | 노랑 | **잉크** | 노랑 5% 규칙 준수 |
| 라운드 | 카드 16px | **14px** | 캐주얼함 감소 |
| 본문 행간 | 1.6 | **1.75** | 리뷰 장문 가독성 |
| 노랑 사용 | "면으로만" | **5% 면적 상한 + 골드 역할 분리** | 배민화 방지 |

⚠️ PRD §3.1의 **"흰 배경에 노란 텍스트 금지"** 원칙은 그대로 유효하며, 아이보리 배경에서도 동일하게 적용된다.

---

# 13. Do / Don't

## ✅ Do

- 여백을 먼저 늘린다
- 노랑 CTA는 화면당 하나
- 제목은 명조, 본문은 Pretendard
- 별점·평점 링은 잉크색
- 사진 아래에 텍스트를 둔다
- 카드는 순백, 배경은 아이보리
- 숫자는 `tabular-nums`
- 훈장에는 획득 연월을 박는다
- 빈 화면에 다음 행동을 준다
- `word-break: keep-all`

## ❌ Don't

- 노랑 배경 헤더·히어로
- 아이보리 위 노란 텍스트
- 본문에 명조
- 18px 미만 명조
- 사진 위 텍스트·필터·틴트
- 그림자 남발
- 카드마다 노란 별점
- 훈장 반짝임·회전 애니메이션
- 느낌표
- 20px 이상 라운드
- 색만으로 상태 구분

---

# 14. 구현 토큰

## 14.1 CSS

```css
:root {
  /* Color */
  --rt-paper: #FDFBF4;      --rt-paper-deep: #F7F3E8;
  --rt-paper-pure: #FFFFFF;
  --rt-ink: #1C1B17;        --rt-ink-70: #55534B;
  --rt-ink-50: #85827A;     --rt-ink-30: #B5B2A8;
  --rt-line: #E5E0D3;       --rt-line-soft: #F0ECE0;
  --rt-yellow: #FFE94F;     --rt-yellow-deep: #EBD33A;
  --rt-yellow-wash: #FFF8CC;
  --rt-gold: #C9A227;       --rt-silver: #9A9A94;
  --rt-bronze: #A9713F;
  --rt-red: #C0392B;        --rt-red-wash: #FBEFED;
  --rt-green: #2F7D4F;      --rt-green-wash: #EDF5F0;

  /* Type */
  --rt-font-display: "Noto Serif KR", "Nanum Myeongjo", serif;
  --rt-font-body: "Pretendard Variable", Pretendard, -apple-system,
                  BlinkMacSystemFont, system-ui, sans-serif;

  /* Space */
  --rt-space-1:4px;  --rt-space-2:8px;   --rt-space-3:12px;
  --rt-space-4:16px; --rt-space-5:24px;  --rt-space-6:32px;
  --rt-space-7:48px; --rt-space-8:64px;  --rt-space-9:96px;
  --rt-space-10:128px;

  /* Radius */
  --rt-radius-sm:6px;  --rt-radius-md:10px; --rt-radius-lg:14px;
  --rt-radius-xl:20px; --rt-radius-full:999px;

  /* Shadow */
  --rt-shadow-sm: 0 1px 2px rgba(28,27,23,.04);
  --rt-shadow-md: 0 4px 16px rgba(28,27,23,.07);
  --rt-shadow-lg: 0 12px 40px rgba(28,27,23,.10);

  /* Motion */
  --rt-dur-fast:150ms; --rt-dur-base:250ms;
  --rt-dur-slow:450ms; --rt-dur-draw:800ms;
  --rt-ease-out: cubic-bezier(.22,1,.36,1);
  --rt-ease-soft: cubic-bezier(.4,0,.2,1);
}

body {
  background: var(--rt-paper);
  color: var(--rt-ink);
  font-family: var(--rt-font-body);
  font-size: 15px;
  line-height: 1.75;
  word-break: keep-all;
  -webkit-font-smoothing: antialiased;
}

h1, h2, .display, .quote {
  font-family: var(--rt-font-display);
  font-weight: 600;
  letter-spacing: -0.02em;
}
```

## 14.2 Tailwind 매핑

```js
// tailwind.config.js
theme: {
  extend: {
    colors: {
      paper:  { DEFAULT:'#FDFBF4', deep:'#F7F3E8', pure:'#FFFFFF' },
      ink:    { DEFAULT:'#1C1B17', 70:'#55534B', 50:'#85827A', 30:'#B5B2A8' },
      line:   { DEFAULT:'#E5E0D3', soft:'#F0ECE0' },
      brand:  { DEFAULT:'#FFE94F', deep:'#EBD33A', wash:'#FFF8CC' },
      gold:'#C9A227', silver:'#9A9A94', bronze:'#A9713F',
      danger:'#C0392B', success:'#2F7D4F',
    },
    fontFamily: {
      display: ['"Noto Serif KR"','"Nanum Myeongjo"','serif'],
      sans: ['"Pretendard Variable"','Pretendard','-apple-system','sans-serif'],
    },
    borderRadius: { sm:'6px', md:'10px', lg:'14px', xl:'20px' },
    boxShadow: {
      sm:'0 1px 2px rgba(28,27,23,.04)',
      md:'0 4px 16px rgba(28,27,23,.07)',
      lg:'0 12px 40px rgba(28,27,23,.10)',
    },
  }
}
```

## 14.3 구현 체크리스트

- [ ] Noto Serif KR 600 KR subset 적용, Windows 폴백 확인
- [ ] Pretendard Variable preload
- [ ] `word-break: keep-all` 전역 적용
- [ ] 노랑 면적 5% 규칙 — 주요 화면 육안 검수
- [ ] 별점·평점 링 잉크색 확인
- [ ] `가게 제공` 라벨 상시 노출 확인
- [ ] 대비 검사 (§3.3 표 기준)
- [ ] `prefers-reduced-motion` 동작 확인
- [ ] 터치 타깃 44px 확인
- [ ] 320px 폭 레이아웃 확인
