# PRD — 리얼 테이블 (Real Table)

> 서울 기념일 맛집, 광고 없는 진짜 후기만.

| 항목 | 내용 |
|---|---|
| **서비스명** | 리얼 테이블 / Real Table |
| **문서 버전** | **v1.0 (최종)** |
| **작성일** | 2026-08-14 |
| **서비스 지역** | 대한민국 **서울특별시 한정** (서울 외 미지원) |
| **현재 스프린트** | 🟡 **Phase 0 — 랜딩페이지** |
| **다음 스프린트** | Phase 0.5 — JSON 기반 프로토타입 |

### 문서 읽는 법

| 표기 | 의미 |
|---|---|
| ✅ | **이번 스프린트 구현 대상** |
| 🔜 | 다음 스프린트 (설계만 확정, 구현 안 함) |
| 📋 | 로드맵 (청사진, 착수 전) |
| ⚠️ | 반드시 지켜야 할 제약 |
| ❓ | 미해결 — 결정 필요 |

---

## 목차

- [1. 서비스 정의](#1-서비스-정의)
- [2. 타깃 사용자](#2-타깃-사용자)
- [3. 브랜드 & 디자인 시스템](#3-브랜드--디자인-시스템)
- [4. 정보 구조 (Information Architecture)](#4-정보-구조-information-architecture)
- [5. 계정 · 익명성 정책](#5-계정-·-익명성-정책)
- [6. 콘텐츠 정책](#6-콘텐츠-정책)
- [7. 리뷰 리스트 · 검색 · 필터 🔜 Phase 0.5](#7-리뷰-리스트-·-검색-·-필터--phase-05)
- [8. 데이터 소스 전략 — JSON → Supabase](#8-데이터-소스-전략--json--supabase)
- [9. 인터랙션 & 애니메이션 🔜 Phase 0.5](#9-인터랙션--애니메이션--phase-05)
- [10. 모바일 최적화 🔜 Phase 0.5](#10-모바일-최적화--phase-05)
- [11. 데이터 대시보드 🔜 Phase 0.5](#11-데이터-대시보드--phase-05)
- [12. 신고 · 제재 시스템 📋 Phase 1](#12-신고-·-제재-시스템--phase-1)
- [13. 관리자 시스템 📋 Phase 1](#13-관리자-시스템--phase-1)
- [14. 빌보드 테이블 📋 Phase 2](#14-빌보드-테이블--phase-2)
- [15. 핵심 기능 목록](#15-핵심-기능-목록)
- [16. 랜딩페이지 명세 ✅ Phase 0 (이번 구현 범위)](#16-랜딩페이지-명세--phase-0-이번-구현-범위)
- [17. 기술 아키텍처](#17-기술-아키텍처)
- [18. 접근성 요건](#18-접근성-요건)
- [19. 법적 · 운영 요건 (출시 전 필수)](#19-법적-·-운영-요건-출시-전-필수)
- [20. 성공 기준](#20-성공-기준)
- [21. 미해결 항목 ❓](#21-미해결-항목)
- [22. 수익 모델](#22-수익-모델)
- [23. 다음 액션](#23-다음-액션)

---

# 1. 서비스 정의

## 1.1 한 문장 정의

> **서울에서 기념일·생일처럼 실패하면 안 되는 날, 20~30대가 광고·협찬 없는 진짜 후기만 보고 확신을 갖고 장소를 정하는 서비스.**

| 축 | 내용 |
|---|---|
| **누가 (Who)** | 서울에서 데이트·모임 장소를 찾는 20~30대 |
| **언제 (When)** | 기념일·생일 등 **실패 비용이 큰 특별한 날**, 장소를 정해야 하는 순간 |
| **왜 (Why)** | 기존 지도·SNS·블로그는 광고와 협찬 리뷰에 오염되어 **믿을 수 있는 판단 근거가 없기 때문** |

## 1.2 네이밍

**Real Table** = 진짜(Real) + 테이블(Table). 광고가 아닌 실제 경험, 그리고 실제로 앉게 될 자리.
⚠️ 상표 선등록 여부 및 도메인 확보 확인 필요 (§19)

## 1.3 문제 정의

| 문제 | 현상 | 결과 |
|---|---|---|
| 정보 신뢰 붕괴 | 검색 상위 대부분이 협찬·대가성 후기 | 무엇을 믿을지 판단 불가 |
| 재시도 불가 | 기념일은 1년에 한 번 | 실패 비용이 비대칭적으로 큼 |
| 결정 지연 | 탭 20개 켜두고 보류 | 결국 익숙한 곳으로 회귀 |
| 정보 과잉 | 검색 결과 수백 건 | 많을수록 확신은 줄어듦 |

**핵심 통찰**: 사용자는 정보가 부족한 게 아니라 **정보를 믿을 수 없어서** 결정을 미룬다.

## 1.4 핵심 가치 제안

> "많은 정보"가 아니라 **"믿을 수 있는 소수의 정보"** 를 제공한다.

## 1.5 제품 원칙 (Product Principles)

기능 결정에서 충돌이 생기면 아래 순서로 판단한다.

1. **광고를 팔지 않는다.** 노출 순위는 어떤 경우에도 금전으로 구매할 수 없다.
2. **익명이되 무책임하지 않다.** 신원은 가리되, 행위는 전부 추적·관리한다.
3. **사진과 글이 전부다.** 자극적 매체(동영상·GIF·음성)를 배제해 정보 밀도를 지킨다.
4. **적게 보여주고 확신을 준다.** 목록의 길이가 아니라 결정의 확실성으로 승부한다.
5. **판단 근거를 공개한다.** 평점 분포, 빌보드 산식, 삭제 사유 — 계산식을 숨기지 않는다.
6. **도구는 팔되 정보는 팔지 않는다.** 편의 기능은 과금 대상이지만, 사용자가 보는 리뷰의 내용·순서·평점은 어떤 결제로도 바뀌지 않는다. (§22.1)

## 1.6 비목표 (Non-Goals)

- 서울 외 지역 서비스 (Phase 4 검토 대상, 현재 명시적 비목표)
- SNS형 팔로우·DM·타임라인 기능
- 배달·포장 정보
- 실시간 웨이팅·예약 (Phase 3 검토)
- 인플루언서·에디터 콘텐츠 유통
- ⚠️ **노출 순위·리뷰 삭제권 판매** (§22.1 — 영구 비목표)

---

# 2. 타깃 사용자

## 2.1 Primary Persona

**김지연 · 29세 · 서울 마포구 · 마케팅 직무**

| 항목 | 내용 |
|---|---|
| 상황 | 3주 뒤 남자친구와 3주년 기념일 |
| 현재 행동 | 인스타 저장 → 블로그 검색 → 어디까지 광고인지 몰라 탭 20개 켜둔 채 보류 |
| 원하는 것 | "여기면 확실하다"는 근거 있는 확신 |
| 두려운 것 | 당일 분위기·맛이 어긋나 특별한 날을 망치는 것 |
| 기기 | iPhone, 출퇴근 지하철에서 탐색 → 주말에 데스크톱으로 최종 확정 |
| 성공 기준 | **10분 안에 후보 3곳으로 좁히고, 그중 1곳을 확신하며 고름** |

## 2.2 Secondary (Phase 2 이후)

- **박준호(34세)** — 상견례 장소를 찾는 사용자. 실패 비용이 더 크고 격식 정보(룸 유무, 조용함)가 핵심
- **이서연(27세)** — 6인 모임 총무. 예산·좌석·접근성 기준으로 후보를 추려 공유해야 함

## 2.3 사용자 여정 (Primary 기준)

```
[인지]  SNS/검색으로 리얼 테이블 발견
   ↓
[탐색]  자치구 + '기념일' 태그로 필터 → 리뷰 리스트 훑기
   ↓
[검증]  상세 진입 → 평점 도넛차트로 분포 확인 → 워드클라우드로 분위기 파악
   ↓        (별점 4.5여도 1점이 몰려 있으면 걸러냄)
[비교]  후보 3곳 저장 → 비교
   ↓
[확신]  최근 리뷰·사진 재확인 → 결정
   ↓
[기여]  방문 후 리뷰 작성 → 리뷰어 등급 상승 → 빌보드 후보
```

⚠️ **검증 단계가 이 서비스의 승부처다.** 대시보드(§11)는 장식이 아니라 이 단계를 위한 핵심 기능이다.

---

# 3. 브랜드 & 디자인 시스템

## 3.1 컬러 토큰

메인은 **바나나(#FFE135)보다 한 단계 밝은 노랑**, 서브는 화이트.

```css
:root {
  /* Brand */
  --rt-yellow:        #FFE94F;  /* Primary. CTA, 브랜드 포인트 */
  --rt-yellow-light:  #FFF6B8;  /* 배지, 호버 배경 */
  --rt-yellow-pale:   #FFFCE8;  /* 대면적 섹션 배경 */
  --rt-yellow-deep:   #E8CE1E;  /* hover/pressed, 보더 강조 */
  --rt-white:         #FFFFFF;

  /* Ink & Gray */
  --rt-ink:           #1A1A17;  /* 기본 텍스트, 노랑 위 텍스트 */
  --rt-gray-700:      #4A4A46;  /* 보조 텍스트 */
  --rt-gray-500:      #6E6E68;  /* 캡션 */
  --rt-gray-400:      #9B9B95;  /* 플레이스홀더, 비활성 */
  --rt-gray-200:      #E7E7E2;  /* 구분선, 카드 보더 */
  --rt-gray-100:      #F5F5F1;  /* 스켈레톤, 비활성 배경 */

  /* Semantic */
  --rt-red:           #E5484D;  /* 신고, 오류, 제재 */
  --rt-red-pale:      #FFF0F0;
  --rt-green:         #2E9E5B;  /* 인증 완료, 성공 */
  --rt-blue:          #3A6FF0;  /* 링크(본문 내) */
}
```

### ⚠️ 노랑 사용 규칙 (접근성 필수)

`--rt-yellow`와 흰색의 명도 대비는 **약 1.3:1로 WCAG AA(4.5:1) 미달**이다.

| 금지 | 허용 |
|---|---|
| ❌ 흰 배경에 노란 텍스트 | ✅ 노란 배경 위 `--rt-ink` 텍스트 (약 15:1) |
| ❌ 노란색 본문·캡션 | ✅ 노란 면(버튼·배경·배지·언더라인) |
| ❌ 노랑만으로 상태 구분 | ✅ 노랑 + 아이콘/텍스트 병행 |
| ❌ 노란 보더로만 포커스 표시 | ✅ `--rt-ink` 2px 아웃라인 + 노랑 글로우 |

**색상 단독으로 정보를 전달하지 않는다.** 평점·상태는 항상 텍스트나 아이콘을 병기한다 (색각 이상 대응).

## 3.2 타이포그래피

- **기본 서체: Pretendard** (Pretendard Variable 우선)
- 라이선스: SIL OFL 1.1 — 웹폰트 임베딩·상업적 이용 가능, 저작권 고지 표기

```css
font-family: "Pretendard Variable", Pretendard, -apple-system,
  BlinkMacSystemFont, system-ui, "Apple SD Gothic Neo",
  "Noto Sans KR", sans-serif;
```

| 스타일 | 모바일 / 데스크톱 | Weight | 자간 | 행간 |
|---|---|---|---|---|
| Display | 32 / 56 | 800 | -0.03em | 1.2 |
| H1 | 26 / 40 | 700 | -0.02em | 1.25 |
| H2 | 20 / 28 | 700 | -0.02em | 1.3 |
| H3 | 17 / 20 | 600 | -0.01em | 1.4 |
| Body | 15 / 16 | 400 | 0 | 1.6 |
| Body Strong | 15 / 16 | 600 | 0 | 1.6 |
| Caption | 13 / 13 | 400 | 0 | 1.5 |
| Numeric | — | 600 | 0 | — |

**로딩 전략**
- self-host + KR subset (전체 폰트 파일 그대로 쓰면 한글 웹폰트는 수 MB)
- `font-display: swap`
- Variable 폰트 우선, 미지원 브라우저는 static 400/600/700/800
- ⚠️ 폰트 스와프로 인한 레이아웃 시프트 방지: `size-adjust` 폴백 조정

**숫자 표기**: 평점·랭킹·통계는 `font-variant-numeric: tabular-nums` (자릿수 정렬)

## 3.3 간격 · 형태 토큰

```css
--rt-space-1: 4px;   --rt-space-2: 8px;   --rt-space-3: 12px;
--rt-space-4: 16px;  --rt-space-5: 24px;  --rt-space-6: 32px;
--rt-space-7: 48px;  --rt-space-8: 64px;  --rt-space-9: 96px;

--rt-radius-sm: 8px;    /* 배지, 태그 */
--rt-radius-md: 12px;   /* 버튼, 인풋 */
--rt-radius-lg: 16px;   /* 카드 */
--rt-radius-xl: 24px;   /* 모달, 바텀시트 */
--rt-radius-full: 999px;

--rt-shadow-sm: 0 1px 3px rgba(26,26,23,0.06);
--rt-shadow-md: 0 2px 12px rgba(26,26,23,0.08);
--rt-shadow-lg: 0 8px 32px rgba(26,26,23,0.12);
```

- 그리드: 모바일 4컬럼 / 태블릿 8 / 데스크톱 12, 최대폭 1200px, 거터 16/24px
- 아이콘: 라인 1.5px 단색(`--rt-ink`), 20px·24px 두 사이즈
- 다크모드: **v1 미지원** (Phase 3)

## 3.4 모션 토큰

```css
--rt-dur-fast:   150ms;  /* 호버, 색 전환 */
--rt-dur-base:   250ms;  /* 대부분의 전환 */
--rt-dur-slow:   400ms;  /* 스크롤 리빌, 모달 */
--rt-dur-slower: 600ms;  /* 차트 드로잉 */

--rt-ease-out:   cubic-bezier(0.22, 1, 0.36, 1);   /* 등장 */
--rt-ease-in:    cubic-bezier(0.64, 0, 0.78, 0);   /* 퇴장 */
--rt-ease-soft:  cubic-bezier(0.4, 0, 0.2, 1);     /* 일반 */
```

## 3.5 핵심 컴포넌트 스펙

| 컴포넌트 | 규격 |
|---|---|
| **Primary Button** | 배경 `--rt-yellow`, 텍스트 `--rt-ink` 600, 높이 48px(모바일)/44px(데스크톱), radius 12px, hover 시 `--rt-yellow-deep` + `translateY(-1px)` |
| **Secondary Button** | 배경 white, 보더 1.5px `--rt-ink`, 텍스트 `--rt-ink` |
| **Ghost Button** | 배경 없음, 텍스트 `--rt-gray-700`, hover 시 `--rt-gray-100` |
| **Input** | 높이 48px, 보더 1.5px `--rt-gray-200`, focus 시 `--rt-ink` 보더 + 노랑 글로우 `0 0 0 3px var(--rt-yellow-light)` |
| **Review Card** | radius 16px, 보더 1px `--rt-gray-200`, 패딩 20px, hover 시 shadow-md + `translateY(-2px)` |
| **Filter Chip** | radius full, 높이 36px, 비활성 white+보더 / 활성 `--rt-yellow` + `--rt-ink` + 체크 아이콘 |
| **Star Rating** | 노랑 채움 + 회색 빈별, **반드시 숫자 병기** (예: ★★★★☆ 4.2) |
| **Badge** | radius full, 높이 24px, 12px 600, 등급별 색상 |
| **Skeleton** | `--rt-gray-100` 배경 + shimmer, `prefers-reduced-motion` 시 정적 |

---

# 4. 정보 구조 (Information Architecture)

## 4.1 화면 목록

| ID | 화면 | 경로 | 단계 |
|---|---|---|---|
| P00 | 랜딩페이지 | `/` (출시 전) | ✅ Phase 0 |
| P01 | 홈 / 리뷰 피드 | `/` | 🔜 Phase 0.5 |
| P02 | 리뷰 리스트 (검색·필터) | `/reviews` | 🔜 Phase 0.5 |
| P03 | 식당 상세 (가게 사진 / 손님 사진 탭 분리) | `/restaurants/[id]` | 🔜 Phase 0.5 |
| P04 | 통계 대시보드 | `/insights` | 🔜 Phase 0.5 |
| P05 | 로그인 / 회원가입 (손님) | `/auth/login`, `/auth/signup` | 📋 Phase 1 |
| P05b | **사장님 회원가입 · 사업자 인증** | `/business/signup` | 📋 Phase 2 |
| P05c | **사장님 콘솔** (가게 관리·대시보드·답변) | `/business` | 📋 Phase 3 |
| P06 | 이메일 인증 안내 | `/auth/verify` | 📋 Phase 1 |
| P07 | 리뷰 작성 / 수정 | `/reviews/write` | 📋 Phase 1 |
| P08 | 내 프로필 | `/me` | 📋 Phase 1 |
| P09 | 저장한 곳 | `/me/saved` | 📋 Phase 2 |
| P10 | 빌보드 테이블 | `/billboard` | 📋 Phase 2 |
| P11 | 신고 모달 | (오버레이) | 📋 Phase 1 |
| A01 | 관리자 대시보드 | `/admin` | 📋 Phase 1 |
| A02 | 신고 큐 | `/admin/reports` | 📋 Phase 1 |
| A03 | 사용자 관리 | `/admin/users` | 📋 Phase 1 |
| A04 | 빌보드 승인 | `/admin/billboard` | 📋 Phase 2 |
| A05 | 감사 로그 | `/admin/logs` | 📋 Phase 1 |

## 4.2 네비게이션

**모바일**: 하단 탭바 5개 — 홈 / 탐색 / 통계 / 빌보드 / 내정보
**데스크톱**: 상단 헤더 (로고 · 검색바 · 메뉴 · 로그인)

---

# 5. 계정 · 익명성 정책

**표면은 익명, 내부는 완전 식별.** 이것이 "진짜 후기"를 보장하는 구조적 장치다.

## 5.1 회원 유형 — 손님 / 사장님 분리

⚠️ 두 유형은 **가입 경로·권한·화면이 완전히 분리**된다. 하나의 계정이 두 역할을 겸할 수 없다.

| 구분 | 🍽 손님 회원 (Guest) | 🏪 사장님 회원 (Owner) |
|---|---|---|
| 가입 | 이메일 + 비밀번호 | 이메일 + 비밀번호 + **사업자등록번호 인증** |
| 표시 이름 | 닉네임 (익명) | 가게명 (실명 노출) |
| 리뷰 작성 | ✅ | ❌ **불가** (이해충돌) |
| 리뷰 신고 | ✅ | ✅ |
| 가게 답변 | ❌ | ✅ (유료, Phase 3) |
| 저장/위시리스트 | ✅ | ❌ |
| 우수 리뷰어 빌보드 | ✅ 대상 | ❌ 대상 아님 |
| 유료 플랜 | 사진 10장 (§22.2) | 관리 도구 (§22.3) |
| 진입 경로 | `/auth/signup` | `/business/signup` |

⚠️ **사장님 계정은 리뷰를 쓸 수 없다.** 자기 가게 셀프 리뷰와 경쟁 가게 악평의 통로가 되기 때문이다. 개인 자격으로 리뷰를 쓰려면 **다른 이메일로 손님 계정**을 별도 가입해야 하며, 두 계정은 시스템상 연결되지 않는다(단, §5.4 내부 모니터링 대상).

## 5.2 손님 회원 가입

- **이메일 + 비밀번호** (v1 기준, 소셜 로그인 미지원 — 익명성 유지 및 SNS 계정 유출 경로 차단)
- **이메일 인증 링크 확인 필수** — 미인증 계정은 리뷰 작성 불가 (조회는 가능)
- **이메일 1개 = 계정 1개**
- 비밀번호: 8자 이상, 영문/숫자/특수문자 중 2종 이상
- 만 14세 이상만 가입 (가입 시 확인 체크)
- 일회용 이메일 도메인 차단 목록 운영

## 5.3 사장님 회원 가입 · 사업자 인증

### 가입 플로우

```
1. 이메일 + 비밀번호 입력
2. 이메일 인증 링크 확인            ← 여기까지는 손님과 동일
3. 사업자등록번호 10자리 입력
     └─ 체크섬 검증 (클라이언트 즉시)
     └─ 국세청 사업자등록 상태 조회 API (휴업·폐업 여부 확인)
4. 대표자명 · 상호 · 개업일 입력
     └─ 국세청 조회 결과와 대조
5. 사업자등록증 사본 업로드 (JPG/PNG/PDF)
6. 담당 가게 선택 or 신규 등록 요청
7. ⚠️ 관리자 심사 (목표 SLA 2영업일)
     ├─ 승인 → 사장님 권한 활성화 + 인증 배지 부여
     └─ 반려 → 사유 통보, 재제출 가능
8. 인증 완료 전까지: 로그인만 가능, 가게 관리 기능 전부 잠금
```

### ⚠️ 자동 승인 금지

사업자등록번호는 **공개 정보**다. 타인의 번호로 가게를 선점하는 사고가 실제로 발생한다.
번호 진위 확인(기계)과 **소유권 확인(사람)** 은 별개이며, 반드시 관리자 승인을 거친다.

| 검증 단계 | 방식 | 확인 대상 |
|---|---|---|
| 1차 | 체크섬 알고리즘 | 번호 형식 유효성 |
| 2차 | 국세청 상태조회 API | 실존·계속사업자 여부 |
| 3차 | 등록증 사본 대조 | 상호·대표자·주소 일치 |
| 4차 | **관리자 승인** | 신청인이 실제 운영자인지 |

### 중복·이관 처리

- **가게 1곳 = 사장님 계정 1개** (`restaurant_owners`의 `restaurant_id` UNIQUE)
- 이미 인증된 가게에 다른 사람이 신청 → 자동 반려 + 관리자 큐 등록 (양도·폐업 후 재개업 케이스 존재)
- 사업자 1인이 **여러 가게** 운영 시: 계정 1개에 가게 복수 연결 허용
- 폐업 확인 시 인증 자동 해제, 획득 훈장은 `(폐업)` 표기 후 아카이브 보존

### 사장님 정보 공개 범위

| 공개 | 비공개 |
|---|---|
| 가게명, 인증 배지 | 대표자명 |
| 획득 훈장 (§14.5) | 사업자등록번호 |
| 가게 답변 (작성 시) | 이메일, 연락처 |

⚠️ 사업자등록번호·대표자명·등록증 사본은 **관리자만 조회**하며, 조회 시 감사 로그를 남긴다(§5.5). 등록증 사본은 승인 후 90일 보관 뒤 파기.

## 5.4 익명성 (손님 회원)


| 공개 | 비공개 |
|---|---|
| 닉네임 | 실명 |
| 아바타 (기본 제공 이미지 중 선택) | 이메일 |
| 리뷰 수, 받은 '도움돼요' 수 | 전화번호 |
| 가입 시기 (월 단위) | 정확한 가입일, IP |
| 리뷰어 등급 배지 | 위치정보, 기기정보 |

- 닉네임: 2~12자, 중복 불가, 금칙어 필터
- ⚠️ **닉네임 변경 30일 1회 제한** — 문제 이력 세탁 방지
- **사용자 간 DM 없음 (v1)** — 개인 접촉을 통한 대가성 거래 차단
- 프로필 사진 업로드 불가, 제공 아바타만 선택 (신원 노출·부적절 이미지 차단)

## 5.5 모니터링 (내부)

익명은 사용자에게만 적용되며, 운영자는 모든 행위를 추적한다.

**저장 항목**
- 영구 `user_id` ↔ 이메일 1:1 매핑 (관리자만 조회)
- 가입/로그인 시각, **IP 해시**, User-Agent, 디바이스 핑거프린트 해시
- 리뷰 작성 시각·소요시간, 수정 이력

**다중계정 탐지 신호**
| 신호 | 임계값 |
|---|---|
| 동일 IP 해시 신규 가입 | 24시간 내 3건 이상 |
| 동일 디바이스 해시 다계정 | 2개 이상 |
| 동일 식당 단기 고평점 집중 | 48시간 내 5건 이상 5.0점 |
| 가입 직후 리뷰 다발 | 가입 24시간 내 3건 이상 |
| 유사 문체 패턴 | 유사도 0.85 이상 |

→ 탐지 시 **자동 플래그 → 관리자 큐 등록** (자동 제재는 하지 않음, 오탐 위험)

**감사**
- 관리자의 개인정보 조회는 **전건 로그 기록 + 사유 입력 필수**
- 감사 로그는 수정·삭제 불가 (append-only)

**탈퇴 처리**
- 개인정보 즉시 파기
- 작성 리뷰는 `탈퇴한 사용자`로 익명 보존 (다른 사용자의 판단 근거 보호)
- 제재 이력은 재가입 차단용으로 해시 형태 보관

## 5.6 리뷰어 등급

| 등급 | 조건 | 혜택 |
|---|---|---|
| 새 테이블 | 가입 직후 | 리뷰 작성 |
| 단골 | 유효 리뷰 5건 | 배지 |
| 믿음직한 테이블 | 유효 리뷰 20건 + 신고 확정 0건 | 배지, 빌보드 후보 자격 |
| 명예의 테이블 | 이달의 우수 리뷰어 3회 | 영구 배지 |

⚠️ 등급은 **식당 노출 순위에 영향을 주지 않는다.** 신뢰 표시 용도다.

---

# 6. 콘텐츠 정책

## 6.0 사진의 두 종류

⚠️ 서비스 내 사진은 **출처가 다른 두 종류**이며, 절대 섞이지 않는다.

| | 손님 리뷰 사진 | 가게 홍보 사진 |
|---|---|---|
| 업로더 | 손님 회원 | 사장님 회원 |
| 장수 | 무료 2장 / 유료 10장 (§22.2) | 무료 6장 / 유료 20장 (§22.3) |
| 라벨 | 없음 | **`가게 제공` 상시 표시** |
| 위치 | `손님 사진` 탭 | `가게 사진` 탭 |
| 통계 집계 | ✅ 포함 | ❌ 제외 |
| 순위 영향 | 없음 | 없음 |
| 허용 포맷 | 동일 (§6.2) | 동일 (§6.2) |

## 6.1 리뷰 구성

| 항목 | 필수 | 규격 |
|---|---|---|
| 별점 | ✔ | 1~5, 0.5 단위 |
| 본문 | ✔ | **최소 30자**, 최대 2,000자 |
| 사진 | 권장 | **무료 2장 / 유료 10장** (§22.2) |
| 방문 시기 | ✔ | 연·월 선택 (최근 12개월 내) |
| 방문 목적 태그 | — | 기념일 / 생일 / 데이트 / 모임 / 상견례 (복수 선택) |
| 분위기 태그 | — | 조용함 / 뷰맛집 / 룸있음 / 대화하기좋음 / 사진잘나옴 |
| 인원 | — | 1~10인 이상 |
| 가격대 | — | 1인 기준 구간 |

## 6.2 업로드 허용 / 금지 포맷

### 허용 — 이미지 전용

| 포맷 | 비고 |
|---|---|
| JPEG (.jpg/.jpeg) | 기본 |
| PNG (.png) | **정적만 — APNG 거부** |
| WebP (.webp) | **정적만 — 애니메이션 WebP 거부** |
| HEIC/HEIF | 서버에서 JPEG 변환 |

- 장당 최대 **10MB**, 최소 640×480
- 자동 처리: 장변 1600px 리사이즈 → WebP 변환 → 썸네일(400px) 생성

### ⚠️ 금지 — 업로드 차단

| 분류 | 확장자 | 차단 사유 |
|---|---|---|
| **GIF** | .gif | 애니메이션 배제, 정보 밀도 저하 |
| **동영상** | .mp4 .mov .avi .webm .mkv .m4v .wmv | 검수 비용, 스팸·유해물 벡터 |
| **음성** | .mp3 .wav .m4a .aac .flac .ogg | 서비스 목적과 무관 |
| 기타 | 문서·압축·실행 파일 전부 | 보안 |

### 차단 구현 요구사항

⚠️ **확장자 검사만으로는 불충분하다.** `video.mp4` → `photo.jpg` 로 이름만 바꾸면 통과한다.

```
1. 클라이언트  accept="image/jpeg,image/png,image/webp,image/heic"
              + 선택 즉시 MIME 1차 검증
2. 서버       매직 넘버(파일 시그니처) 검사
              JPEG: FF D8 FF  /  PNG: 89 50 4E 47  /  WebP: RIFF....WEBP
              GIF(47 49 46 38) 및 영상·음성 시그니처 발견 시 즉시 거부
3. 서버       애니메이션 프레임 수 검사 → 2프레임 이상 거부
              (APNG acTL 청크, WebP ANIM 청크 확인)
4. 서버       이미지 재인코딩(re-encode) — 임베디드 페이로드 제거
5. 서버       Content-Type 강제 지정 후 저장 (스토리지가 원본 MIME 신뢰 금지)
```

**사용자 오류 메시지**
> `사진(JPG·PNG·WebP)만 올릴 수 있어요. GIF·동영상·음성 파일은 지원하지 않습니다.`

## 6.3 개인정보 · 저작권 처리

- ⚠️ 업로드 이미지의 **EXIF 전체 제거** (GPS 좌표 포함) — 익명성 보호를 위한 필수 처리
- 타인의 얼굴이 식별되는 사진 금지 (신고 사유)
- 타 사이트 이미지 무단 게시 금지 → 신고 시 즉시 블라인드
- 객체 스토리지 저장, 원본 URL은 비추측(unguessable) 경로

## 6.4 금지 콘텐츠

광고·협찬 후기 / 허위 방문 / 욕설·비방 / 음란·혐오 / 개인정보 노출 / 도배·중복 / 리뷰와 무관 / 외부 홍보 링크 / 대가성 거래 제안

---

# 7. 리뷰 리스트 · 검색 · 필터 🔜 Phase 0.5

리얼 테이블의 **탐색 엔진**. 사용자가 "10분 안에 후보 3곳으로 좁히는" 것을 목표로 설계한다.

## 7.1 화면 레이아웃

**모바일 (< 768px)**
```
┌─────────────────────────┐
│ 🔍 검색바 (sticky)       │
├─────────────────────────┤
│ [기념일][데이트][강남] ▸ │ ← 가로 스크롤 칩
│ ⚙ 필터 (3)   ↕ 최신순    │ ← 탭 시 바텀시트
├─────────────────────────┤
│ 총 128개                 │
├─────────────────────────┤
│ ┌─────────────────────┐ │
│ │ [사진] 식당명 ★4.5   │ │
│ │ 강남구 · 기념일       │ │
│ │ 리뷰 본문 2줄...      │ │
│ │ 닉네임 · 2일 전       │ │
│ └─────────────────────┘ │
│ (카드 1열, 무한 스크롤)  │
└─────────────────────────┘
```

**데스크톱 (≥ 1024px)**
```
┌──────────┬──────────────────────────┐
│ 필터     │ 🔍 검색바      ↕ 정렬     │
│ 사이드바 ├──────────────────────────┤
│ (sticky) │ 총 128개                  │
│          │ ┌────────┐ ┌────────┐   │
│ 자치구   │ │ 카드   │ │ 카드   │   │
│ 목적     │ └────────┘ └────────┘   │
│ 별점     │ ┌────────┐ ┌────────┐   │
│ 가격대   │ │ 카드   │ │ 카드   │   │
│ 사진유무 │ └────────┘ └────────┘   │
│          │      (2열 그리드)         │
│ [초기화] │      [더 보기]            │
└──────────┴──────────────────────────┘
```

## 7.2 검색 사양

| 항목 | 내용 |
|---|---|
| 검색 대상 | 식당명, 리뷰 본문, 메뉴명, 자치구·동 |
| 입력 방식 | 실시간 (디바운스 **300ms**) |
| 최소 글자 | 1자 (한글 특성상 2자 제한은 과함) |
| 결과 하이라이트 | 매칭 키워드 `--rt-yellow-light` 배경 |
| 최근 검색어 | 로컬 저장 최대 10개, 삭제 가능 |
| 추천 검색어 | 인기 자치구·태그 (하드코딩 → Phase 1에서 집계 기반) |
| 빈 결과 | 일러스트 + "필터를 줄여보세요" + 전체 초기화 버튼 + 인기 검색어 3개 |

### ⚠️ 한국어 검색 처리

```
1. 정규화     NFC 통일 (macOS는 NFD로 입력됨 — '한글' 검색이 안 되는 원인)
2. 공백·특수문자 제거 후 비교
3. 대소문자 무시 (영문 식당명 대응)
4. 부분 일치  '스시' → '스시조', '오마카세스시'  모두 매칭
5. 초성 검색  ❓ Phase 1 검토 ('ㅅㅅ' → '스시')
```

⚠️ **Supabase 전환 시 주의**: PostgreSQL 기본 전문검색(`tsvector`)은 **한국어 형태소 분석을 지원하지 않는다.** `pg_trgm` 확장의 trigram 유사도 검색을 사용하거나, 검색 대상 컬럼을 미리 정규화한 인덱스 컬럼으로 두는 방식을 택한다. (§17.5)

## 7.3 필터 사양

| 필터 | 타입 | 옵션 | 기본값 |
|---|---|---|---|
| **자치구** | 다중 선택 | 서울 25개구 | 전체 |
| **방문 목적** | 다중 선택 | 기념일/생일/데이트/모임/상견례 | 전체 |
| **분위기** | 다중 선택 | 조용함/뷰맛집/룸있음/대화좋음/사진잘나옴 | 전체 |
| **별점** | 단일 선택 | 4.5↑ / 4.0↑ / 3.5↑ / 전체 | 전체 |
| **가격대** | 범위 | 1만↓ / 1~3만 / 3~5만 / 5~10만 / 10만↑ | 전체 |
| **사진 있는 리뷰만** | 토글 | on/off | off |
| **최근 리뷰만** | 토글 | 6개월 이내 | off |

**필터 결합 로직**
- 필터 **간**: AND (자치구 AND 목적 AND 별점)
- 필터 **내**: OR (강남구 OR 마포구)
- 활성 필터 수를 버튼에 배지로 표시 → `⚙ 필터 (3)`
- 개별 칩 X로 해제 + 전체 초기화 버튼

**정렬**
| 옵션 | 기준 |
|---|---|
| 최신순 (기본) | 리뷰 작성일 DESC |
| 평점 높은순 | 평균 별점 DESC, 동점 시 리뷰 수 DESC |
| 리뷰 많은순 | 리뷰 수 DESC |
| 도움돼요순 | 도움돼요 수 DESC (Phase 2) |

⚠️ **"추천순" 정렬은 만들지 않는다.** 불투명한 순위는 광고 의혹을 낳고, 제품 원칙 1·5와 충돌한다.

## 7.4 URL 동기화

필터·검색 상태를 쿼리스트링에 반영해 **공유·북마크·뒤로가기**를 지원한다.

```
/reviews?q=오마카세&gu=강남구,서초구&purpose=기념일&rating=4.5&photo=1&sort=latest&page=2
```

- 뒤로가기 시 이전 필터·**스크롤 위치 복원**
- 새로고침해도 상태 유지
- 필터 변경 시 `history.replaceState` (히스토리 오염 방지), 검색 실행 시 `pushState`

## 7.5 페이지네이션

| 화면 | 방식 |
|---|---|
| 모바일 | 무한 스크롤 (IntersectionObserver, 하단 200px 도달 시 로드) |
| 데스크톱 | "더 보기" 버튼 (스크롤 하이재킹 방지, 푸터 접근성 확보) |
| 페이지당 | 20건 |
| 로딩 표시 | 스켈레톤 카드 3개 |
| 끝 도달 | "모든 리뷰를 확인했어요" + 필터 초기화 제안 |

⚠️ 리스트 항목 200개 초과 시 **가상 스크롤(virtualization)** 도입 검토 — 모바일 DOM 부하

## 7.6 리뷰 카드 구성

| 요소 | 내용 |
|---|---|
| 썸네일 | 대표 사진 1장 (16:9), 없으면 노랑 플레이스홀더 + 식당명 이니셜 |
| 사진 개수 | `📷 5` 배지 (2장 이상일 때) |
| 식당명 | H3, 1줄 말줄임 |
| 별점 | 별 + **숫자 병기** `★ 4.5` |
| 위치 | 자치구 · 동 |
| 목적 태그 | 최대 2개 칩, 초과 시 `+2` |
| 본문 | 2줄 말줄임 (`-webkit-line-clamp: 2`) |
| 작성자 | 닉네임 + 등급 배지 |
| 작성일 | 상대 시간 ("2일 전"), 7일 초과 시 절대 날짜 |
| 액션 | 저장(Phase 2), 신고(Phase 1) |

---

# 8. 데이터 소스 전략 — JSON → Supabase

## 8.1 2단계 전환 원칙

| 단계 | 데이터 소스 | 목적 |
|---|---|---|
| **Phase 0.5** | **정적 JSON** (`/data/reviews.json`) | 백엔드 없이 UI·검색·필터·대시보드 완성 및 검증 |
| **Phase 1** | **Supabase** (PostgreSQL) | 실제 계정·작성·권한·운영 |

⚠️ **핵심 설계 규칙**: JSON 단계의 데이터 구조를 **Supabase 테이블 스키마와 1:1로 일치**시킨다. 데이터 접근을 `lib/data/` 레이어로 추상화해 두면, 전환 시 **해당 레이어 구현만 교체**하고 UI 코드는 손대지 않는다.

```ts
// lib/data/reviews.ts — 인터페이스는 고정, 구현만 교체
export interface ReviewRepository {
  list(params: ReviewQuery): Promise<Paged<Review>>;
  getById(id: string): Promise<Review | null>;
  getStats(restaurantId?: string): Promise<ReviewStats>;
}

// Phase 0.5: JsonReviewRepository   (fetch + 클라이언트 필터)
// Phase 1  : SupabaseReviewRepository (쿼리 빌더)
```

## 8.2 JSON 스키마 (Phase 0.5)

```jsonc
// /public/data/restaurants.json
{
  "restaurants": [
    {
      "id": "rst_001",
      "name": "테이블 포 투",
      "category": "이탈리안",
      "gu": "강남구",
      "dong": "신사동",
      "address": "서울특별시 강남구 도산대로 00길 00",
      "price_range": "5-10",           // 만원 단위 구간 코드
      "avg_rating": 4.6,
      "review_count": 42,
      "atmosphere_tags": ["조용함", "룸있음", "사진잘나옴"],
      "thumbnail": "/images/rst_001_01.webp",
      "status": "open"
    }
  ]
}
```

```jsonc
// /public/data/reviews.json
{
  "reviews": [
    {
      "id": "rev_00001",
      "restaurant_id": "rst_001",
      "author": {
        "nickname": "조용한테이블",
        "avatar_id": "av_07",
        "grade": "trusted"            // new | regular | trusted | honored
      },
      "rating": 4.5,
      "body": "3주년 기념일에 방문했어요. 룸으로 안내받아서...",
      "images": [
        "/images/rev_00001_01.webp",
        "/images/rev_00001_02.webp"
      ],
      "visit_year_month": "2026-06",
      "purpose_tags": ["기념일", "데이트"],
      "atmosphere_tags": ["조용함", "룸있음"],
      "party_size": 2,
      "price_per_person": 78000,
      "helpful_count": 23,
      "created_at": "2026-06-14T19:20:00+09:00",
      "status": "published"           // published | blinded | deleted
    }
  ],
  "meta": {
    "total": 128,
    "generated_at": "2026-08-14T00:00:00+09:00"
  }
}
```

**더미 데이터 요건**
- 리뷰 최소 **120건**, 식당 최소 **30곳** (필터 조합·차트가 의미 있으려면 필요한 최소 규모)
- 서울 25개 자치구 중 **최소 10개구** 분포
- 별점 분포는 현실적으로: 5점 35%, 4점 40%, 3점 15%, 2점 7%, 1점 3%
- 사진 없는 리뷰 30% 포함 (플레이스홀더 렌더 검증)
- 본문 길이 30자~2000자 다양하게 (말줄임·워드클라우드 검증)
- ⚠️ 실존 식당명·실제 리뷰 무단 사용 금지 — 가상 데이터로 생성

## 8.3 전환 체크리스트 (Phase 0.5 → 1)

- [ ] JSON 필드명 = Supabase 컬럼명 일치 확인
- [ ] 클라이언트 필터 로직 → SQL `WHERE` 절로 이관
- [ ] 클라이언트 정렬 → `ORDER BY` + 인덱스
- [ ] 페이지네이션 → `range()` 기반 커서
- [ ] 통계 집계 → DB View 또는 사전 집계 테이블
- [ ] 이미지 경로 → Supabase Storage URL

---

# 9. 인터랙션 & 애니메이션 🔜 Phase 0.5

## 9.1 원칙

1. **의미 있는 움직임만.** 장식적 애니메이션은 넣지 않는다.
2. **60fps 유지.** `transform`과 `opacity`만 애니메이션한다. `width`·`height`·`top`·`left` 금지 (리플로우 유발).
3. ⚠️ **`prefers-reduced-motion` 필수 대응.** 전정기관 장애 사용자에게 모션은 실제 불편을 유발한다.
4. **모바일에서는 절제.** 스크롤 성능이 곧 체감 품질이다.

```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }
}
```

## 9.2 스크롤 애니메이션

| 효과 | 대상 | 사양 |
|---|---|---|
| **Fade-up Reveal** | 섹션, 카드 | `opacity 0→1`, `translateY(24px→0)`, 400ms, `--rt-ease-out` |
| **Stagger** | 카드 그리드 | 자식 요소 60ms 간격 순차 등장, 최대 8개까지만 (그 이상은 동시) |
| **Sticky Header** | 상단 헤더 | 80px 스크롤 시 축소 + shadow-sm, 250ms |
| **Sticky Filter Bar** | 검색·필터 | 리스트 상단 고정, 스크롤 다운 시 숨김 / 업 시 표시 |
| **Progress Bar** | 상세 페이지 | 읽기 진행률, 노랑 4px 바 |
| **Count-up** | 통계 숫자 | 뷰포트 진입 시 0→목표값, 600ms, `tabular-nums`로 폭 고정 |
| **Chart Draw** | 도넛차트 | 아크 순차 그리기, 600ms |
| **Back to Top** | FAB | 400px 스크롤 시 페이드인 |

**구현 원칙**
- `IntersectionObserver` 사용 (스크롤 이벤트 리스너 금지 — 성능)
- `threshold: 0.15`, `rootMargin: '0px 0px -80px 0px'`
- **1회 실행 후 unobserve** (재진입 시 반복 애니메이션 금지 — 산만함)
- 초기 뷰포트 내 요소는 애니메이션 없이 즉시 표시 (LCP 손해 방지)

## 9.3 호버 애니메이션 (데스크톱 전용)

| 대상 | 효과 |
|---|---|
| 리뷰 카드 | `translateY(-2px)` + shadow-sm→md, 150ms |
| 카드 썸네일 | 내부 이미지 `scale(1.04)`, overflow hidden, 250ms |
| Primary 버튼 | 배경 `--rt-yellow`→`--rt-yellow-deep` + `translateY(-1px)` |
| 필터 칩 | 배경 `--rt-gray-100`, 보더 `--rt-ink` |
| 텍스트 링크 | 노랑 언더라인 좌→우 확장 (`scaleX`) |
| 차트 세그먼트 | 해당 아크 3px 확장 + 툴팁 |
| 별점 | 개별 별 확대 (작성 화면) |
| 이미지 갤러리 | 확대 아이콘 페이드인 |

⚠️ **터치 기기에서 호버 효과 비활성화**
```css
@media (hover: hover) and (pointer: fine) {
  .card:hover { transform: translateY(-2px); }
}
```
터치 기기에서 `:hover`는 탭 후 잔류해 어색한 상태를 만든다. 대신 `:active` 로 `scale(0.98)` 피드백을 준다.

## 9.4 상태 전환

| 상황 | 처리 |
|---|---|
| 데이터 로딩 | **스켈레톤** (스피너 아님 — 레이아웃 시프트 방지) |
| 필터 적용 | 리스트 `opacity 0.5` → 결과 fade-in, 250ms |
| 무한 스크롤 | 하단 스켈레톤 3개 |
| 모달/바텀시트 | 배경 fade + 시트 슬라이드업, 300ms |
| 토스트 | 하단 슬라이드업, 3초 후 자동 소멸 |
| 폼 오류 | 인풋 shake 200ms + 빨간 보더 + 텍스트 메시지 (색상 단독 금지) |
| 페이지 전환 | 콘텐츠 영역만 fade, 150ms |

---

# 10. 모바일 최적화 🔜 Phase 0.5

⚠️ **모바일 우선.** Primary Persona는 지하철에서 탐색한다. 데스크톱은 확장이지 기준이 아니다.

## 10.1 브레이크포인트

| 이름 | 범위 | 레이아웃 |
|---|---|---|
| Mobile S | 320~374px | 1열, 여백 16px |
| Mobile | 375~767px | 1열, 여백 20px |
| Tablet | 768~1023px | 2열, 여백 24px |
| Desktop | 1024~1439px | 2열 + 필터 사이드바 |
| Wide | 1440px~ | 3열, 최대폭 1200px 중앙 정렬 |

**최소 지원 폭: 320px** (iPhone SE 1세대 기준)

## 10.2 터치 대응

- ⚠️ **최소 터치 타깃 44×44px** (WCAG 2.5.5) — 시각적으로 작아도 히트 영역 확보
- 인접 터치 요소 간 최소 8px 간격
- 스와이프: 이미지 갤러리 좌우, 바텀시트 하단 드래그로 닫기
- 하단 탭바는 `env(safe-area-inset-bottom)` 적용 (iPhone 홈 인디케이터)
- 고정 헤더는 `env(safe-area-inset-top)` 적용 (노치)
- `touch-action: manipulation` — 300ms 탭 지연 제거
- ⚠️ **입력 폼 `font-size: 16px` 이상** — iOS Safari가 그 이하에서 자동 확대함

## 10.3 모바일 전용 패턴

| 요소 | 데스크톱 | 모바일 |
|---|---|---|
| 필터 | 좌측 사이드바 (sticky) | **바텀시트** (전체 높이 80%) |
| 정렬 | 드롭다운 | 바텀시트 |
| 네비게이션 | 상단 헤더 | **하단 탭바** (엄지 도달 범위) |
| 검색 | 헤더 인라인 | 전용 화면 전환 |
| 이미지 갤러리 | 그리드 + 라이트박스 | 가로 스와이프 캐러셀 |
| 페이지네이션 | 더 보기 버튼 | 무한 스크롤 |
| CTA | 인라인 | **하단 고정 버튼** (스크롤 무관 노출) |

## 10.4 성능 예산

| 지표 | 목표 | 조건 |
|---|---|---|
| LCP | ≤ 2.5s | 4G, 모바일 |
| INP | ≤ 200ms | — |
| CLS | ≤ 0.1 | — |
| TTI | ≤ 3.5s | 4G |
| 초기 JS 번들 | ≤ 200KB (gzip) | — |
| 초기 이미지 총량 | ≤ 500KB | 첫 화면 기준 |
| Lighthouse (모바일) | Performance ≥ 90, Accessibility ≥ 95 | — |

**최적화 수단**
- 이미지: WebP, `loading="lazy"`, `width`/`height` 명시(CLS 방지), `srcset` 반응형
- 폰트: KR subset, `font-display: swap`, preload
- 차트 라이브러리는 **동적 import** (대시보드 진입 시에만 로드)
- 워드클라우드는 **사전 계산된 결과 사용** (클라이언트 형태소 분석 금지 — §11.3)
- 리스트 200개 초과 시 가상 스크롤

---

# 11. 데이터 대시보드 🔜 Phase 0.5

⚠️ 대시보드는 **장식이 아니라 검증 도구**다. "별점 4.5"라는 숫자 하나로는 확신할 수 없다. **분포**를 보여줘야 판단이 선다.

## 11.1 대시보드 위치

| 위치 | 범위 | 목적 |
|---|---|---|
| **식당 상세 페이지 내** | 해당 식당 | 이 가게를 믿을지 판단 |
| **`/insights` 전용 페이지** | 서비스 전체 | 서울 전체 트렌드 탐색 |
| **관리자 대시보드** | 운영 지표 | 신고·어뷰징 모니터링 (Phase 1) |

## 11.2 도넛차트

### 차트 1 — 별점 분포 (식당 상세)

| 항목 | 내용 |
|---|---|
| 데이터 | 1~5점 각 비율 |
| 중앙 표시 | 평균 별점 (Display 32px) + 총 리뷰 수 |
| 색상 | 5점 `--rt-yellow` → 1점 `--rt-gray-200` **단계적 명도** |
| 인터랙션 | 세그먼트 호버 시 확장 + 툴팁(`4점 · 17건 · 40%`), 클릭 시 해당 별점 리뷰만 필터 |
| 애니메이션 | 뷰포트 진입 시 600ms 아크 드로잉 |
| 범례 | 우측(데스크톱) / 하단(모바일), 색상 칩 + 라벨 + 건수 |

⚠️ **왜 중요한가**: 평균 4.5여도 5점 90% + 1점 10%면 "호불호가 갈리는 곳"이다. 기념일에는 이런 곳을 피해야 한다. 평균값만으로는 이걸 알 수 없다.

### 차트 2 — 방문 목적 분포 (식당 상세)

기념일/생일/데이트/모임/상견례 비율. **"기념일 68%"** 는 이 서비스에서 가장 강력한 신호다.

### 차트 3 — 자치구별 리뷰 분포 (`/insights`)

상위 8개구 + 기타. 클릭 시 해당 구 필터로 리스트 이동.

### 차트 4 — 가격대 분포 (`/insights`)

예산 기준 탐색 진입점.

### 공통 사양

- 라이브러리: **Recharts** 또는 **Chart.js** (❓ 확정 필요)
- 반응형: 모바일 지름 200px / 데스크톱 280px
- ⚠️ **접근성**: 차트 옆 또는 접이식 영역에 **동일 데이터를 표 형태로 제공**. 스크린리더는 SVG 도넛을 읽지 못한다. `role="img"` + `aria-label` 요약 병기.
- ⚠️ 5% 미만 세그먼트는 라벨 생략 후 툴팁으로만 표시 (겹침 방지)
- 데이터 0건 시: 빈 상태 일러스트 + "아직 리뷰가 없어요"

## 11.3 워드클라우드

### 목적

리뷰 본문에서 **자주 언급되는 키워드**를 추출해 "이 가게가 어떤 곳인지"를 3초 만에 전달한다. 리뷰 42개를 다 읽지 않아도 "조용한", "룸", "직원분", "플레이팅"이 크게 보이면 성격이 파악된다.

### 사양

| 항목 | 내용 |
|---|---|
| 데이터 소스 | 해당 식당 리뷰 본문 전체 (또는 `/insights`에서 전체) |
| 추출 방식 | **명사 중심 키워드 빈도** |
| 표시 개수 | 상위 30~40개 |
| 폰트 크기 | 빈도 비례, 최소 12px ~ 최대 40px (**제곱근 스케일** — 선형은 편차가 과하게 벌어짐) |
| 색상 | 빈도 상위 `--rt-ink` → 하위 `--rt-gray-400`, 최상위 3개만 `--rt-yellow-deep` |
| 배치 | 나선형(archimedean spiral), 겹침 방지 |
| 인터랙션 | 호버 시 확대 + 언급 횟수 툴팁, 클릭 시 해당 키워드 포함 리뷰 필터 |
| 애니메이션 | 뷰포트 진입 시 중앙에서 페이드+스케일 확산, stagger 30ms |
| 라이브러리 | `d3-cloud` 또는 `react-wordcloud` (❓ 확정 필요) |

### ⚠️ 한국어 처리 — 가장 큰 기술 리스크

영어는 공백으로 단어가 나뉘지만 **한국어는 조사가 붙는다.** 단순 공백 분리 시 `분위기가`, `분위기는`, `분위기도`가 전부 별개 단어로 집계되어 워드클라우드가 무의미해진다.

**처리 파이프라인**
```
1. 정규화       NFC 통일, 특수문자·이모지 제거
2. 형태소 분석  명사(NNG/NNP) 추출
                라이브러리: mecab-ko / khaiii / 경량 대안 es-hangul
                ❓ 서버 사이드 실행 필요 — 클라이언트 형태소 분석은 번들 과대
3. 불용어 제거  하다, 있다, 되다, 없다, 것, 수, 등, 저희, 진짜, 정말,
                너무, 그냥, 좀, 조금, 다음, 이번, 오늘, 어제 …
4. 동의어 통합  '직원분/스탭/서버' → '직원'
                '분위기/무드' → '분위기'
                '맛있다/맛있어요/맛있음' → '맛'
5. 최소 빈도    2회 이상 언급된 키워드만 (1회는 노이즈)
6. 최소 글자    2자 이상
7. 상위 40개 선정
```

⚠️ **성능 결정**: 형태소 분석을 페이지 진입 시마다 수행하면 안 된다.
- **Phase 0.5**: 사전 계산해 JSON에 `keywords` 배열로 포함 → 클라이언트는 렌더링만
- **Phase 1**: Supabase Edge Function에서 **야간 배치** 집계 → `restaurant_keywords` 테이블 저장

```jsonc
// 사전 계산 결과 예시
{
  "restaurant_id": "rst_001",
  "keywords": [
    { "word": "분위기", "count": 31 },
    { "word": "룸",     "count": 24 },
    { "word": "직원",   "count": 19 },
    { "word": "플레이팅", "count": 15 }
  ],
  "computed_at": "2026-08-14T03:00:00+09:00"
}
```

### 접근성

⚠️ 워드클라우드는 스크린리더로 읽을 수 없다. **반드시 "키워드 목록 보기" 토글**을 제공해 `키워드 · 언급 횟수` 표를 제공한다.

## 11.4 보조 지표 카드

대시보드 상단 요약 (Count-up 애니메이션 적용)

| 카드 | 내용 |
|---|---|
| 총 리뷰 수 | 128 |
| 평균 별점 | 4.3 |
| 사진 첨부율 | 72% |
| 최근 30일 신규 리뷰 | 18 |

---

# 12. 신고 · 제재 시스템 📋 Phase 1

## 12.1 신고

- 모든 리뷰·사진·닉네임에 신고 버튼
- 사유(택 1): **광고·협찬 의심 / 허위 방문 / 욕설·비방 / 음란·혐오 / 개인정보 노출 / 도배·중복 / 리뷰와 무관 / 저작권 침해 / 기타(직접 입력)**
- 동일 사용자의 동일 콘텐츠 중복 신고 불가
- 신고자 신원은 피신고자에게 비공개
- 허위·악의적 반복 신고자도 제재 대상

## 12.2 처리 플로우

```
신고 접수
  └─ 누적 3건 도달  or  중대 사유(음란/개인정보/저작권) 1건
       └─ 자동 블라인드 (즉시 비공개, 삭제 아님)
            └─ 관리자 검토 (목표 SLA 24시간)
                 ├─ 위반 확정 → 삭제 + 작성자 제재 + 양측 통보
                 └─ 위반 아님 → 블라인드 해제 + 복원 + 신고자 통보
                      └─ 작성자 이의제기 (7일 내, 1회)
```

- 삭제 리뷰는 화면에서 제거하되 **DB 소프트 삭제로 보존** (분쟁·수사 대응, 3년)
- ⚠️ 삭제 사유를 작성자에게 **반드시 통보** (정보통신망법)

## 12.3 제재 단계

| 단계 | 조치 | 기준 |
|---|---|---|
| 1 | 경고 알림 | 경미한 위반 1회 |
| 2 | 리뷰 작성 7일 제한 | 위반 확정 2회 |
| 3 | 리뷰 작성 30일 제한 | 위반 확정 3회 |
| 4 | 영구 이용 제한 | 위반 확정 4회 또는 중대 위반 1회 |

**즉시 영구 제재**: 대가성 리뷰 작성·알선, 조직적 평점 조작, 음란물 게시, 타인 개인정보 유포

## 12.4 가게 측 대응

- ⚠️ 가게는 **광고를 구매할 수 없다.** 노출 순위 판매 없음.
- ⚠️ **리뷰 삭제 요청권은 유료로도 판매하지 않는다** (§22.3). 유료 사장님은 이의제기를 **더 빨리 검토받을 뿐**, 삭제 판정 기준은 §6.4 위반 여부로 동일하다.
- 이의제기 접수: 무료 사장님 이메일(SLA 7일) / 유료 사장님 전용 폼(SLA 24시간)
- ⚠️ **별점이 낮다는 사실 자체는 삭제 사유가 아니다.**
- 이의제기 처리 결과와 사유는 **리뷰 작성자에게도 통보**한다
- 사업자 확인 후 **가게 답변 1회** 등록 가능 (유료, Phase 3)

---

# 13. 관리자 시스템 📋 Phase 1

## 13.1 권한 등급

| 역할 | 권한 |
|---|---|
| **슈퍼관리자** | 전체. 관리자 계정 생성·삭제, 개인정보 조회, 감사 로그 열람 |
| **운영관리자** | 콘텐츠 삭제, 사용자 제재, 빌보드 승인, 식당 정보 수정 |
| **모더레이터** | 신고 큐 처리, 블라인드/해제, 제재 **건의**(확정 불가) |

- ⚠️ 관리자 계정은 **초대 방식으로만 생성** (공개 가입 경로 없음)
- ⚠️ 관리자 로그인 **2FA 필수**
- 모든 관리자 액션은 `누가·언제·무엇을·왜` 감사 로그 기록 (append-only, 수정·삭제 불가)
- 관리자 세션 타임아웃 30분

## 13.2 백오피스 화면

| 화면 | 기능 |
|---|---|
| 대시보드 | 신규 가입, 리뷰 수, **미처리 신고 건수**, 자동 플래그 알림, 일별 추이 차트 |
| **신고 큐** | 우선순위 정렬, 원문·사진·작성자 이력 한 화면, 삭제/복원/제재 원클릭 |
| 리뷰 관리 | 검색·필터, 강제 블라인드, 일괄 처리 |
| 사용자 관리 | 계정 조회, 제재 이력, **다중계정 의심 그룹**, 활동 타임라인 |
| 식당 관리 | 등록·수정·폐업, 중복 병합 |
| **사업자 인증 심사** | 등록증 대조, 소유권 확인, 승인/반려 (SLA 2영업일) |
| **훈장 관리** | 부여 내역, 회수 처리, 키트 구매 자격 검증 |
| **빌보드 관리** | 월간 자동 집계 결과 검토 → **승인 후 공개** |
| 감사 로그 | 관리자 행위 전체 열람 (슈퍼관리자 전용) |

---

# 14. 빌보드 테이블 📋 Phase 2

매월 **이달의 우수 리뷰어**와 **이달의 베스트 식당**을 선정하는 명예의 전당.

## 14.1 공통 규칙

- 집계: 매월 1일 00:00 KST, 직전 월(1일~말일) 데이터
- **자동 집계 → 관리자 검토 → 승인 후 공개** (조작·오선정 방지, 목표 공개 매월 3일)
- 과거 회차 아카이브 영구 보존
- ⚠️ **선정에 금전적 대가 개입 불가.** 가게는 후보 신청조차 할 수 없다.
- ⚠️ **선정 산식을 공개한다** (제품 원칙 5)

## 14.2 이달의 우수 리뷰어 (Top 5)

**자격 (전부 충족)**
- 이메일 인증 완료
- 해당 월 유효 리뷰 3건 이상
- 해당 월 신고 확정 이력 0건
- 현재 제재 상태 아님
- 직전 2개월 내 선정 이력 없음 (독점 방지)

**점수 산식**
```
점수 = (유효 리뷰 수 × 10)
     + (받은 '도움돼요' 수 × 3)
     + (사진 1장 이상 첨부 리뷰 **비율** × 20)   ⚠️ 장수 아님 — 유료 플랜 유불리 차단
     + (평균 본문 길이 점수, 최대 15)
     − (신고 접수 건수 × 5)
```
- 동점: 도움돼요 수 → 사진 첨부율 → 가입 순
- 노출: 닉네임, 아바타, 등급 배지, 대표 리뷰 1건, 월간 리뷰 수 (**개인정보 일절 미노출**)
- 리워드: 프로필 영구 배지, 아카이브 등재. ⚠️ **금전 보상 없음** — 리뷰 양산 유인 차단

## 14.3 이달의 베스트 식당 (Top 10)

**자격 (전부 충족)**
- 서울특별시 소재, 영업 중
- 해당 월 신규 리뷰 5건 이상
- **서로 다른 리뷰어 5인 이상**
- 누적 평점 4.0 이상
- 해당 월 조작 의심 플래그 없음

**점수 산식**
```
점수 = [ (해당 월 평균 별점 × 20)
       + (해당 월 신규 리뷰 수 × 5, 최대 50)
       + (고유 리뷰어 수 × 3)
       + (저장 수 증가분 × 2) ]
       × 신뢰 가중치
```
`신뢰 가중치`: 동일 IP/디바이스 리뷰 비율이 높을수록 감산 (최대 0.5배까지 하향)

- 동점: 고유 리뷰어 수 → 평균 별점 → 저장 수
- 노출: 식당명, 대표 사진, 자치구, 평균 별점, 월간 리뷰 수, 대표 리뷰 1건
- 카테고리별 시상(기념일/데이트/모임 부문 분리)은 Phase 3 검토

## 14.4 빌보드 화면

- 상단: 월 선택 드롭다운 (아카이브 탐색)
- 좌: 우수 리뷰어 Top 5 — 1위 `--rt-yellow` 강조, 순위 배지
- 우: 베스트 식당 Top 10 — 사진 우선 카드
- 하단: **선정 기준 전문 링크**
- 애니메이션: 순위 카드 stagger 등장, 1위 카드만 노랑 글로우

## 14.5 훈장 배지 — 금 · 은 · 동 🏅

빌보드 선정 결과를 **영구 훈장**으로 전환해, 한 달짜리 순위를 지속적인 신뢰 자산으로 만든다.

### 등급 기준

| 훈장 | 획득 조건 | 색상 |
|---|---|---|
| 🥇 **금** | 이달의 베스트 식당 **1위** | `#D4AF37` |
| 🥈 **은** | 이달의 베스트 식당 **2~3위** | `#A8A9AD` |
| 🥉 **동** | 이달의 베스트 식당 **4~10위** | `#B87333` |
| 👑 **명예의 테이블** | 금 훈장 **3회** 누적 | `--rt-yellow-deep` |

### ⚠️ 훈장은 판매하지 않는다

훈장은 **손님이 남긴 리뷰 데이터로만** 결정된다. 유료 플랜 가입 여부는 획득 조건에 **일절 포함되지 않으며**, 무료 사장님 계정 가게도, **사장님 계정이 아예 없는 가게도** 동일하게 획득한다.

> 유료 회원만 훈장을 받는 순간, 훈장은 "좋은 가게 표시"가 아니라 "돈 낸 가게 표시"가 된다.
> 사용자가 그 사실을 알아채는 즉시 배지의 가치는 0이 되고, 팔 물건 자체가 사라진다.
> **훈장은 공짜로 주고, 훈장을 활용하는 물건을 판다** (§22.3 대안 A+).

### 표시 규칙

| 위치 | 표시 |
|---|---|
| 식당 카드 | 최고 등급 훈장 1개 + 누적 개수 `🥇×3` |
| 식당 상세 상단 | 훈장 전체 + **획득 연월** |
| 빌보드 아카이브 | 회차별 수상 내역 |
| 검색 결과 | 훈장 아이콘 (⚠️ **정렬 가중치 없음**) |

⚠️ **획득 시점을 반드시 병기한다.** `🥇 2026.07` 처럼 표기하지 않으면, 2년 전 금 훈장이 현재 실력처럼 읽혀 사용자를 오도한다.
⚠️ 훈장은 **정렬·노출 순위에 영향을 주지 않는다.** 표시만 한다.

### 회수 조건

| 상황 | 처리 |
|---|---|
| 폐업 확인 | 훈장에 `(폐업)` 표기, 아카이브 보존 |
| 리뷰 조작 확정 | **훈장 회수** + 빌보드 기록 삭제 + 공지 |
| 최근 6개월 평점 3.5 미만 | 훈장 흐리게 표시 + "최근 평가는 다를 수 있어요" 안내 |
| 사업자 변경(양도) | 훈장 승계 ❌ — 이전 운영자 기록으로 아카이브 이관 |

```sql
create table restaurant_medals (
  id            uuid primary key default gen_random_uuid(),
  restaurant_id uuid not null references restaurants(id) on delete cascade,
  tier          text not null,          -- gold | silver | bronze
  year_month    text not null,          -- 'YYYY-MM'
  rank          int  not null,
  revoked_at    timestamptz,
  revoked_reason text,
  created_at    timestamptz not null default now(),
  unique (restaurant_id, year_month)
);
```

---

# 15. 핵심 기능 목록

> ⚠️ **범위 경고**
> 아래는 서비스 전체 청사진이다.
> **이번 스프린트(Phase 0)에서 구현하는 것은 §16의 랜딩페이지가 전부이며, 아래 기능은 단 하나도 구현하지 않는다.**
> 랜딩페이지는 이 기능들을 *약속*할 뿐 *제공*하지 않는다.

| # | 기능 | 설명 | 단계 |
|---|---|---|---|
| **F0** | **랜딩페이지** | 가치 제안 + 사전 등록 수집 | ✅ **Phase 0** |
| **F1** | **리뷰 리스트** | 카드형 목록, 무한 스크롤/더보기 | 🔜 Phase 0.5 |
| **F2** | **검색** | 식당명·본문·메뉴·지역, 디바운스 300ms | 🔜 Phase 0.5 |
| **F3** | **필터 · 정렬** | 자치구/목적/분위기/별점/가격대/사진유무, URL 동기화 | 🔜 Phase 0.5 |
| **F4** | **JSON 데이터 레이어** | 정적 JSON + Repository 추상화 | 🔜 Phase 0.5 |
| **F5** | **스크롤·호버 애니메이션** | IntersectionObserver 리빌, 호버 인터랙션, reduced-motion 대응 | 🔜 Phase 0.5 |
| **F6** | **모바일 최적화** | 반응형, 바텀시트, 하단 탭바, 터치 44px, safe-area | 🔜 Phase 0.5 |
| **F7** | **도넛차트 대시보드** | 별점 분포, 목적 분포, 자치구·가격대 분포 | 🔜 Phase 0.5 |
| **F8** | **워드클라우드** | 리뷰 키워드 추출·시각화 (사전 계산) | 🔜 Phase 0.5 |
| **F9** | 식당 상세 페이지 | 정보·사진·리뷰·대시보드 | 🔜 Phase 0.5 |
| **F10** | **Supabase Auth** | 이메일 회원가입·로그인·인증메일·비번재설정 | 📋 Phase 1 |
| **F11** | **Supabase DB 전환** | JSON → PostgreSQL, RLS 적용 | 📋 Phase 1 |
| **F12** | **Supabase Storage** | 리뷰 이미지 업로드, 포맷 검증, EXIF 제거 | 📋 Phase 1 |
| **F13** | 리뷰 작성·수정·삭제 | 별점+글+사진, 동영상·GIF·음성 불가 | 📋 Phase 1 |
| **F14** | 신고 시스템 | 접수 → 자동 블라인드 → 관리자 검토 | 📋 Phase 1 |
| **F15** | 관리자 백오피스 | 신고 큐, 삭제, 제재, 감사 로그 | 📋 Phase 1 |
| **F16** | 저장 / 위시리스트 | 후보 모아 비교 | 📋 Phase 2 |
| **F17** | **빌보드 테이블** | 이달의 우수 리뷰어 · 베스트 식당 | 📋 Phase 2 |
| **F18** | 도움돼요 · 리뷰어 등급 | 리뷰 품질 신호 | 📋 Phase 2 |
| **F19** | 가게 답변 | 사업자 확인 후 1회, 유료 사장님 전용 | 📋 Phase 3 |
| **F20** | **이용자 플랜** | 사진 무료 2장 / 유료 10장, 서버 검증 | 📋 Phase 2 |
| **F21** | **사장님 계정 · 사업자 인증** | 손님 계정과 분리 가입, 국세청 API + 등록증 + 관리자 승인 | 📋 Phase 2 |
| **F21b** | **훈장 배지 (금·은·동)** | 빌보드 성적 기반 자동 부여, 무료·유료 무관 | 📋 Phase 2 |
| **F21c** | **훈장 활용 키트** | 실물 스티커·명패·PDF·임베드 위젯 (유료) | 📋 Phase 3 |
| **F21d** | **가게 홍보 사진 관리** | 무료 6장 / 유료 20장, `가게 제공` 라벨, 탭 분리 | 📋 Phase 2 |
| **F22** | **사장님 대시보드** | 별점 분포·워드클라우드·목적 분포·월별 추이 | 📋 Phase 3 |
| **F23** | 구독 결제 | PG 연동, 정기결제, 해지·환불 | 📋 Phase 2 |

---

# 16. 랜딩페이지 명세 ✅ Phase 0 (이번 구현 범위)

## 16.1 목표

| 항목 | 내용 |
|---|---|
| 1차 목표 | 사전 등록 이메일 확보 |
| 2차 목표 | "광고 없는 진짜 후기" 포지셔닝 각인 |
| 핵심 지표 | 방문 → 사전 등록 전환율 |
| 우선 디바이스 | **모바일 우선** |
| 언어 | 한국어 전용 |

## 16.2 섹션 구성

| # | 섹션 | 목적 | 구성 요소 |
|---|---|---|---|
| S1 | **Hero** | 3초 안에 정체성 전달 | 로고, 헤드라인, 서브카피, 이메일 입력+CTA, `서울특별시 전용` 배지. 배경 `--rt-yellow-pale`, CTA `--rt-yellow`+`--rt-ink` |
| S2 | **문제 제기** | 공감 | "검색하면 다 광고 같을 때" 상황 묘사 3줄, fade-up |
| S3 | **우리의 원칙** | 차별점 | 원칙 3카드 (광고를 팔지 않는다 / 익명이되 무책임하지 않다 / 사진과 글이 전부다), stagger 등장 |
| S4 | **이럴 때 씁니다** | 사용 맥락 | 기념일·생일·특별한 날 3컷 |
| S5 | **신뢰 구조** | 믿을 근거 | 이메일 인증 → 익명 작성 → 신고·관리자 검수 → 부정 리뷰 삭제 플로우 다이어그램 |
| S6 | **미리 보는 인사이트** | 기능 차별성 시연 | **도넛차트 + 워드클라우드 목업** (더미 데이터, ⚠️ "예시" 명시). 뷰포트 진입 시 드로잉 애니메이션 |
| S7 | **빌보드 테이블 예고** | 기대감 | 우수 리뷰어·베스트 식당 목업 (⚠️ "예시" 명시) |
| S8 | **출시 로드맵** | 진행감 | Phase 0.5~2 요약 (구체 날짜 미기재) |
| S9 | **사전 등록 CTA** | 전환 | 이메일 폼 재노출 + 개인정보 수집 동의 체크박스(필수) |
| S10 | **FAQ** | 이탈 방지 | 아코디언 |
| S11 | **Footer** | 마무리 | 서비스명, 문의 이메일, 개인정보처리방침, 이용약관, Pretendard 라이선스 고지 |

## 16.3 Hero 카피

- **헤드라인 A**: `오늘은 실패하면 안 되는 날이니까`
- **헤드라인 B**: `광고 빼고, 진짜 테이블만`
- **서브카피**: `서울에서 기념일·생일에 갈 곳, 협찬 없는 후기로만 고르세요.`
- **CTA**: `사전 등록하고 먼저 받아보기`
- **보조 문구**: `광고를 팔지 않습니다. 순위는 돈으로 살 수 없습니다.`

## 16.4 FAQ 항목

1. 서울만 되나요? → 네, 서울특별시 전용입니다.
2. 무료인가요? → 리뷰 조회·작성 모두 무료입니다.
3. 익명인데 어떻게 신뢰하나요? → 이메일 인증 + 신고 + 관리자 검수 구조를 설명
4. 동영상은 못 올리나요? → 사진과 글에 집중합니다. GIF·동영상·음성은 지원하지 않습니다.
5. 가게가 광고를 살 수 있나요? → 불가능합니다. **순위와 리뷰 삭제는 판매하지 않습니다.**
6. 유료 회원은 뭐가 다른가요? → 리뷰 사진을 10장까지 올릴 수 있습니다. 노출·순위·평점에는 어떤 영향도 없습니다.
6. 언제 출시되나요? → 사전 등록하시면 가장 먼저 알려드립니다.

## 16.5 사전 등록 폼

- 수집 항목: **이메일 1개만**
- 필수: 개인정보 수집·이용 동의 체크박스 (미체크 시 제출 불가)
- 동의 문구: `출시 알림 발송 목적으로 이메일을 수집하며, 출시 후 3개월 또는 수신거부 시 파기합니다.`
- 클라이언트·서버 양측 형식 검증, 중복 제출 방지
- 스팸 방지: honeypot 필드 + rate limit (동일 IP 시간당 5회)
- 성공 시: 인라인 성공 메시지 + 체크 애니메이션 (페이지 이동 없음)

## 16.6 Out of Scope (Phase 0)

❌ 실제 식당 데이터·검색·리스트 ❌ 회원가입/로그인 ❌ 리뷰 조회·작성 ❌ 신고 ❌ 관리자 ❌ 실제 빌보드 집계 ❌ 실제 통계(더미 목업만) ❌ Supabase 연동 ❌ 지도 ❌ 결제 ❌ 앱 ❌ 다크모드

## 16.7 기술 요건

- 정적 사이트(SSG), 반응형 320px~
- Pretendard self-host + KR subset
- 이메일 저장: 외부 폼 서비스 또는 Supabase `waitlist` 테이블 (❓ 확정)
- SEO: title/description, OG 이미지(노랑 배경+로고), `<html lang="ko">`, JSON-LD
- 성능: LCP ≤ 2.5s, Lighthouse 90+
- 접근성: 폼 label 연결, 키보드 조작, 대비 규칙(§3.1), `prefers-reduced-motion`

---

# 17. 기술 아키텍처

## 17.1 스택

| 레이어 | 선택 | 비고 |
|---|---|---|
| 프레임워크 | Next.js (App Router) | ❓ 확정 필요 |
| 언어 | TypeScript | strict |
| 스타일 | Tailwind CSS + CSS 변수 토큰 | §3 토큰을 `theme`에 매핑 |
| 폰트 | Pretendard (self-host) | OFL 1.1 |
| 차트 | Recharts 또는 Chart.js | ❓ 확정 필요, 동적 import |
| 워드클라우드 | d3-cloud | 사전 계산 결과 렌더만 |
| 애니메이션 | CSS + IntersectionObserver | 라이브러리 최소화 |
| **BaaS** | **Supabase** | Auth · PostgreSQL · Storage · Edge Functions |
| 배포 | Vercel | ❓ 확정 필요 |

## 17.2 Supabase 구성 📋 Phase 1

| Supabase 기능 | 용도 |
|---|---|
| **Auth** | 이메일+비밀번호 가입, 인증 메일, 비밀번호 재설정, 세션 |
| **Database** | 리뷰·식당·신고·제재·빌보드 데이터 |
| **Storage** | 리뷰 이미지 (버킷 `review-images`) |
| **Row Level Security** | 권한 제어의 핵심 |
| **Edge Functions** | 이미지 검증, 워드클라우드 배치, 빌보드 집계 |
| **Realtime** | ❌ 미사용 (v1) |

## 17.3 데이터 모델

```sql
-- 프로필 (auth.users와 1:1)
create table profiles (
  id            uuid primary key references auth.users on delete cascade,
  nickname      text unique not null check (char_length(nickname) between 2 and 12),
  avatar_id     text not null default 'av_01',
  grade         text not null default 'new',      -- new|regular|trusted|honored
  status        text not null default 'active',   -- active|suspended|banned
  review_count  int  not null default 0,
  helpful_count int  not null default 0,
  nickname_changed_at timestamptz,
  created_at    timestamptz not null default now()
);

-- 내부 모니터링 (사용자에게 절대 노출 금지)
create table user_signals (
  user_id       uuid primary key references profiles(id) on delete cascade,
  ip_hash       text,
  device_hash   text,
  last_login_at timestamptz,
  flag_count    int not null default 0
);

create table restaurants (
  id            uuid primary key default gen_random_uuid(),
  name          text not null,
  category      text,
  gu            text not null,      -- 서울 25개구
  dong          text,
  address       text not null,
  price_range   text,               -- '1-3' | '3-5' | '5-10' | '10+'
  status        text not null default 'open',
  avg_rating    numeric(2,1) not null default 0,
  review_count  int not null default 0,
  search_text   text,               -- 정규화 검색용 (§17.5)
  created_at    timestamptz not null default now()
);

create table reviews (
  id                uuid primary key default gen_random_uuid(),
  user_id           uuid not null references profiles(id),
  restaurant_id     uuid not null references restaurants(id),
  rating            numeric(2,1) not null check (rating between 1 and 5),
  body              text not null check (char_length(body) between 30 and 2000),
  visit_year_month  text not null,          -- 'YYYY-MM'
  purpose_tags      text[] default '{}',
  atmosphere_tags   text[] default '{}',
  party_size        int,
  price_per_person  int,
  helpful_count     int not null default 0,
  report_count      int not null default 0,
  status            text not null default 'published', -- published|blinded|deleted
  deleted_reason    text,
  created_at        timestamptz not null default now(),
  updated_at        timestamptz,
  deleted_at        timestamptz
);

create table review_images (
  id          uuid primary key default gen_random_uuid(),
  review_id   uuid not null references reviews(id) on delete cascade,
  storage_key text not null,
  width       int, height int,
  sort_order  int not null default 0
);

create table reports (
  id          uuid primary key default gen_random_uuid(),
  reporter_id uuid not null references profiles(id),
  target_type text not null,        -- review|image|nickname
  target_id   uuid not null,
  reason      text not null,
  detail      text,
  status      text not null default 'pending',  -- pending|resolved|rejected
  handled_by  uuid, handled_at timestamptz, result text,
  created_at  timestamptz not null default now(),
  unique (reporter_id, target_type, target_id)   -- 중복 신고 차단
);

create table sanctions (
  id        uuid primary key default gen_random_uuid(),
  user_id   uuid not null references profiles(id),
  level     int  not null,          -- 1~4
  reason    text not null,
  starts_at timestamptz not null default now(),
  ends_at   timestamptz,            -- null = 영구
  issued_by uuid not null,
  appeal_status text default 'none'
);

create table admin_users (
  id    uuid primary key references auth.users on delete cascade,
  role  text not null,              -- super|operator|moderator
  created_at timestamptz not null default now()
);

-- append-only. update/delete 정책 자체를 만들지 않는다.
create table admin_logs (
  id          bigserial primary key,
  admin_id    uuid not null,
  action      text not null,
  target_type text, target_id uuid,
  reason      text not null,
  created_at  timestamptz not null default now()
);

create table restaurant_keywords (
  restaurant_id uuid references restaurants(id) on delete cascade,
  word          text not null,
  count         int  not null,
  computed_at   timestamptz not null default now(),
  primary key (restaurant_id, word)
);

create table billboards (
  id           uuid primary key default gen_random_uuid(),
  year_month   text not null,       -- 'YYYY-MM'
  type         text not null,       -- reviewer|restaurant
  rank         int  not null,
  target_id    uuid not null,
  score        numeric not null,
  approved_by  uuid,
  published_at timestamptz,
  unique (year_month, type, rank)
);

create table waitlist (
  id           uuid primary key default gen_random_uuid(),
  email        text unique not null,
  consented_at timestamptz not null,
  ip_hash      text,
  created_at   timestamptz not null default now()
);
```

## 17.4 RLS 정책 (핵심)

⚠️ **모든 테이블에 RLS를 켠다.** Supabase는 anon key가 클라이언트에 노출되므로, RLS 없이는 테이블이 전부 공개된다.

```sql
alter table reviews enable row level security;

-- 조회: 공개된 리뷰만 누구나
create policy "public read published"
  on reviews for select
  using (status = 'published');

-- 작성: 이메일 인증 + 제재 없는 본인만
create policy "verified users insert own"
  on reviews for insert
  with check (
    auth.uid() = user_id
    and (auth.jwt() ->> 'email_confirmed_at') is not null
    and not exists (
      select 1 from sanctions s
      where s.user_id = auth.uid()
        and (s.ends_at is null or s.ends_at > now())
        and s.level >= 2
    )
  );

-- 수정: 본인 + 작성 후 24시간 이내
create policy "author update own within 24h"
  on reviews for update
  using (auth.uid() = user_id and created_at > now() - interval '24 hours');
```

**정책 요약표**

| 테이블 | 익명 | 로그인 | 본인 | 관리자 |
|---|---|---|---|---|
| `reviews` | published만 조회 | 조회 | 작성/수정(24h)/삭제 | 전체 + 상태변경 |
| `profiles` | 공개 필드만 | 공개 필드 | 본인 전체 수정 | 전체 |
| `user_signals` | ❌ | ❌ | ❌ | 슈퍼관리자만 |
| `reports` | ❌ | 본인 신고 생성 | 본인 것 조회 | 전체 |
| `sanctions` | ❌ | 본인 것 조회 | — | 전체 |
| `admin_logs` | ❌ | ❌ | ❌ | 슈퍼관리자 조회만 |
| `waitlist` | insert만 | — | — | 전체 |

⚠️ `user_signals`, `admin_logs`는 **service_role로만 접근**한다. 클라이언트 키로는 정책 자체를 만들지 않는다.

## 17.5 검색 인덱스

```sql
create extension if not exists pg_trgm;

-- 한국어 형태소 분석 미지원 → trigram 유사도 검색 사용
create index idx_restaurants_search on restaurants using gin (search_text gin_trgm_ops);
create index idx_reviews_body on reviews using gin (body gin_trgm_ops);

-- 필터·정렬 인덱스
create index idx_reviews_restaurant on reviews (restaurant_id, created_at desc);
create index idx_reviews_status_created on reviews (status, created_at desc);
create index idx_reviews_purpose on reviews using gin (purpose_tags);
create index idx_restaurants_gu on restaurants (gu, avg_rating desc);
```

## 17.6 Storage 정책

```
버킷: review-images (public read, authenticated write)

⚠️ Supabase Storage의 allowedMimeTypes 설정만으로는 부족하다.
   클라이언트가 Content-Type 헤더를 위조할 수 있다.

업로드 경로:
  클라이언트 → Edge Function(검증) → Storage
  직접 업로드 금지

Edge Function 검증 순서:
  1. 파일 크기 ≤ 10MB
  2. 매직 넘버 검사 (JPEG/PNG/WebP만 통과)
  3. GIF·MP4·MP3 등 시그니처 발견 시 즉시 거부
  4. 애니메이션 프레임 수 검사 (2프레임 이상 거부)
  5. EXIF 전체 제거
  6. 리사이즈 + WebP 재인코딩
  7. Content-Type 강제 지정 후 저장
```

## 17.7 Edge Functions

| 함수 | 트리거 | 역할 |
|---|---|---|
| `validate-upload` | 이미지 업로드 시 | §17.6 검증 파이프라인 |
| `compute-keywords` | 매일 03:00 KST cron | 형태소 분석 → `restaurant_keywords` 갱신 |
| `compute-billboard` | 매월 1일 00:00 KST cron | 빌보드 점수 집계 → 관리자 승인 대기 |
| `detect-abuse` | 리뷰 작성 시 | 다중계정·어뷰징 신호 판정 → 플래그 |
| `refresh-stats` | 리뷰 CUD 시 트리거 | `avg_rating`, `review_count` 갱신 |

## 17.8 보안 체크리스트

- [ ] 모든 테이블 RLS 활성화 확인 (테이블 추가 시마다 재확인)
- [ ] `service_role` 키는 **서버 사이드에만** 존재 (`NEXT_PUBLIC_` 접두사 절대 금지)
- [ ] 관리자 판별을 클라이언트 상태로 하지 않음 — DB 조회로만
- [ ] 이미지 업로드 Edge Function 경유 강제
- [ ] Rate limit: 리뷰 작성 시간당 5건, 신고 시간당 10건, 사전등록 IP당 5회
- [ ] 리뷰 본문 XSS — 렌더 시 sanitize, HTML 허용 안 함
- [ ] 비밀번호 재설정 토큰 만료 1시간
- [ ] CORS 화이트리스트

---

# 18. 접근성 요건

⚠️ 다음은 권장이 아니라 **필수**다.

| 항목 | 요건 |
|---|---|
| 명도 대비 | 본문 4.5:1, 큰 텍스트 3:1 이상. 노랑 사용 규칙 §3.1 준수 |
| 색상 단독 정보 전달 | 금지. 별점·상태·오류는 텍스트/아이콘 병기 |
| 키보드 | 전 기능 조작 가능, 논리적 탭 순서, 포커스 링 항상 표시 |
| 포커스 트랩 | 모달·바텀시트 내부에 포커스 가둠, ESC 닫기 |
| 스크린리더 | 이미지 alt, 폼 label 연결, `aria-live`로 검색 결과 수 안내 |
| **차트** | 도넛차트·워드클라우드는 **동등한 표 형태 대체 콘텐츠 필수** |
| 모션 | `prefers-reduced-motion` 대응 |
| 터치 타깃 | 최소 44×44px |
| 언어 | `<html lang="ko">` |
| 목표 | **WCAG 2.1 AA**, Lighthouse Accessibility ≥ 95 |

---

# 19. 법적 · 운영 요건 (출시 전 필수)

- [ ] **개인정보처리방침** 작성·게시 (개인정보보호법)
- [ ] **이용약관** — 리뷰 삭제 기준, 제재 기준, 저작권 이용허락 범위 명시
- [ ] 개인정보 수집·이용 동의 절차 (랜딩 폼 포함)
- [ ] 개인정보보호책임자 지정 및 연락처 공개
- [ ] 만 14세 미만 가입 차단
- [ ] ⚠️ 콘텐츠 삭제 시 작성자 통지 절차 (정보통신망법)
- [ ] 게시물 임시조치(블라인드) 절차 문서화
- [ ] ⚠️ **식당 정보 출처의 적법성** — 무단 크롤링 금지, 공공데이터 또는 직접 수집
- [ ] ⚠️ **더미 데이터에 실존 상호·실제 리뷰 사용 금지**
- [ ] 상표 검색 — "리얼 테이블 / Real Table" 선등록 여부
- [ ] 도메인 확보
- [ ] Pretendard OFL 라이선스 고지 (Footer)
- [ ] 차트·워드클라우드 라이브러리 라이선스 확인 (MIT/BSD 여부)
- [ ] ⚠️ **통신판매업 신고** (유료 플랜 도입 시)
- [ ] ⚠️ 정기결제 사전 고지·자동갱신 안내 의무 (전자상거래법)
- [ ] ⚠️ 유료 사장님 이의제기 처리 기준을 **공개 문서화** — "돈 낸 가게 리뷰만 지운다"는 의혹 차단
- [ ] 사업자등록번호 수집에 따른 개인정보처리방침 반영
- [ ] ⚠️ **훈장 명칭·디자인 상표 등록** — 미획득 가게의 무단 사용 대응 근거
- [ ] ⚠️ 훈장 키트 실물 판매 시 **통신판매업 신고 + 청약철회 규정** 적용
- [ ] ⚠️ **투명성 리포트** 정례 공개 (이의제기 접수·인용 건수) — 삭제 의혹 사전 차단
- [ ] 사업자등록증 사본 보관·파기 정책 (승인 후 90일)
- [ ] ⚠️ **가게 홍보 사진의 `가게 제공` 라벨 상시 노출** — 미표시 시 기만적 표시 소지 (표시광고법)
- [ ] ⚠️ 실제 미제공 메뉴·공간 사진 게시에 대한 사장님 고지 및 약관 반영

---

# 20. 성공 기준

## Phase 0 (랜딩) 완료 조건
- [ ] S1~S11 전 섹션 구현
- [ ] 320px~1440px 정상 렌더링
- [ ] 사전 등록 폼 수집 + 중복·스팸 방어
- [ ] `prefers-reduced-motion` 대응 확인
- [ ] Lighthouse Performance ≥ 90, Accessibility ≥ 95
- [ ] 배포 및 공개 URL 접근 가능

## Phase 0.5 (프로토타입) 완료 조건
- [ ] JSON 120건 이상 더미 데이터로 리스트 렌더
- [ ] 검색 + 필터 6종 + 정렬 3종 정상 동작, 조합 결과 정확
- [ ] URL 쿼리 동기화 및 뒤로가기 상태 복원
- [ ] 무한 스크롤 / 더 보기 동작
- [ ] 스크롤 리빌·호버 애니메이션 60fps 유지
- [ ] 도넛차트 4종 + 워드클라우드 렌더 및 인터랙션
- [ ] 차트 대체 표 제공 (접근성)
- [ ] 모바일 바텀시트 필터 동작
- [ ] Repository 레이어 분리로 Supabase 전환 준비 완료

## Phase 1 (MVP) 완료 조건
- [ ] Supabase Auth 가입·인증메일·로그인·비번재설정
- [ ] RLS 전 테이블 적용 및 침투 테스트 통과
- [ ] 리뷰 작성·이미지 업로드 (GIF/동영상/음성 차단 검증 포함)
- [ ] 신고 → 자동 블라인드 → 관리자 삭제 전 과정
- [ ] 관리자 백오피스 + 감사 로그

## 지표

| 지표 | Phase |
|---|---|
| 사전 등록 수 | 0 |
| 랜딩 전환율 | 0 |
| 검색→상세 진입률 | 0.5 |
| 필터 사용률 | 0.5 |
| 가입 완료율(인증까지) | 1 |
| 리뷰 작성 전환율 | 1 |
| 신고 처리 SLA 준수율 (24h) | 1 |
| 리뷰 삭제율 (건전성 지표) | 1 |

---

# 21. 미해결 항목 ❓

| # | 항목 | 비고 |
|---|---|---|
| 1 | 요금 수준 | 이용자 플랜 / 사장님 플랜 월 금액, 훈장 키트 단가 미정 |
| 1-b | 국세청 사업자등록 상태조회 API | 공공데이터포털 신청·쿼터 확인 필요 |
| 1-c | 훈장 실물 제작 | 스티커·명패 제작처 및 배송 단가 |
| 2 | **초기 식당 데이터 확보** | 직접 큐레이션 vs 공공데이터. 무단 크롤링은 배제 |
| 3 | 영수증 인증 도입 여부 | 신고+관리자 검수로 부족할 경우 |
| 4 | 프레임워크·배포 확정 | Next.js / Vercel 가정 중 |
| 5 | 차트 라이브러리 확정 | Recharts vs Chart.js |
| 6 | 워드클라우드 형태소 분석기 | mecab-ko / khaiii / 경량 대안 |
| 7 | 초성 검색 지원 여부 | Phase 1 검토 |
| 8 | 사전 등록 목표 수치 | Phase 1 착수 기준 |
| 9 | 로고·심볼·OG 이미지 | 디자인 필요 |
| 10 | 도메인·상표 확보 결과 | 블로커 가능성 |

---

# 22. 수익 모델

⚠️ 이 챕터는 제품 원칙(§1.5)과 가장 강하게 충돌할 수 있는 영역이다. **원칙 1 "광고를 팔지 않는다"** 와 **원칙 5 "판단 근거를 공개한다"** 를 기준으로 판매 가능/불가를 구분한다.

## 22.1 판매 가능 / 판매 불가 경계

| 구분 | 기준 | 예시 |
|---|---|---|
| ✅ **판매 가능** | 사용자의 **판단 근거를 왜곡하지 않는** 편의·도구 | 사진 장수 확대, 통계 대시보드, 관리 도구, 응답 채널 |
| ❌ **판매 불가** | 사용자가 보는 **정보 자체를 바꾸는** 권한 | 리뷰 삭제·숨김, 노출 순위, 평점 보정, 빌보드 선정 |

> **판별 질문**: "이 기능을 산 가게와 안 산 가게가 있을 때, 사용자가 보는 리뷰의 내용이나 순서가 달라지는가?"
> 달라진다면 판매 불가다.

---

## 22.2 이용자 플랜 — 리뷰 사진 장수 차등 ✅ 확정

| 플랜 | 리뷰당 사진 | 비고 |
|---|---|---|
| **무료** | **최대 2장** | 리뷰 작성·조회·검색·대시보드 전부 무제한 이용 |
| **유료** | **최대 10장** | 사진 장수만 확대 |

### 설계 근거

기념일 맛집 리뷰에서 사진은 **본문보다 강한 증거**다. 코스 요리 6종, 룸 내부, 플레이팅, 외관을 다 보여주려면 2장으로는 부족하다. 사진을 많이 올리고 싶은 사용자는 **기여 의지가 높은 사용자**이므로, 과금 대상으로 자연스럽다.

### ⚠️ 반드시 지킬 제약

| 제약 | 이유 |
|---|---|
| 무료 사용자도 **리뷰 작성 자체는 무제한** | 유료화가 리뷰 총량을 줄이면 서비스가 죽는다 |
| 사진 **0장 리뷰도 정상 노출** | 텍스트 리뷰의 가치를 훼손하지 않음 |
| 정렬·검색에 **사진 장수 가중치 금지** | 돈으로 노출을 사는 것과 같아짐 |
| 리뷰에 **유료 회원 배지 표시 금지** | 익명성 훼손 + "돈 낸 사람 후기가 더 믿을 만하다"는 잘못된 신호 |
| 빌보드 산식은 **사진 첨부 여부(0/1)** 만 반영 | 장수를 반영하면 유료 회원이 구조적으로 유리해짐 (§14.2 반영 완료) |
| 플랜 해지 시 기존 리뷰 사진 **유지** | 이미 게시된 판단 근거를 사후에 삭제하면 사용자 기만 |

### 구현 사양

```
업로드 화면
  무료: "사진 2/2 · 유료 플랜에서 10장까지 올릴 수 있어요" (비강압적 안내)
  유료: "사진 5/10"

서버 검증 (⚠️ 클라이언트 제한만으로 불충분)
  1. review_images insert 전 profiles.plan 조회
  2. 무료 && count > 2  → 거부
  3. 유료 && count > 10 → 거부
  4. 플랜 만료 시점 검증 (plan_expires_at > now())
```

```sql
alter table profiles
  add column plan text not null default 'free',   -- free | plus
  add column plan_expires_at timestamptz;

create or replace function max_photos(p_user uuid) returns int as $$
  select case
    when plan = 'plus' and (plan_expires_at is null or plan_expires_at > now())
    then 10 else 2 end
  from profiles where id = p_user;
$$ language sql stable;
```

---

## 22.3 사장님 플랜 — 훈장 활용 키트 + 관리 도구 ✅ 확정

### 판매 가능 / 불가 재확인

| 항목 | 판매 | 이유 |
|---|---|---|
| 사업자 인증 배지 | ❌ 무료 제공 | 신원 확인은 신뢰 인프라, 과금 대상 아님 |
| **훈장 획득 자격** | ❌ **판매 불가** | 유료만 받으면 "좋은 가게 표시"가 "돈 낸 가게 표시"로 변질 |
| 리뷰 삭제 요청권 | ❌ **판매 불가** | 법적 리스크 + 제품 자멸 (하단 근거) |
| **훈장 활용 키트** | ✅ **판매** | 훈장은 공짜, 훈장을 쓰는 물건은 유료 |
| **가게 홍보 사진 장수** | ✅ 판매 | 라벨·영역 분리로 리뷰와 혼동되지 않음, 순위 무영향 |
| 가게 관리 도구 | ✅ 판매 | 사용자가 보는 정보 왜곡 없음 |
| 사장님 대시보드 | ✅ 판매 | 사장님에게만 제공되는 인사이트 |
| 가게 공식 답변 | ✅ 판매 | 답변은 리뷰를 지우지 않고 **덧붙인다** |

### 플랜 구성

| 기능 | 무료 사장님 | 유료 사장님 |
|---|---|---|
| 사업자 인증 배지 | ✅ | ✅ |
| 🏅 **훈장 획득·표시** | ✅ | ✅ *(동일)* |
| 신규 리뷰 알림 | ✅ (주 1회 요약) | ✅ (실시간) |
| **훈장 활용 키트** | ❌ | ✅ |
| **가게 홍보 사진** | **6장** | **20장** |
| 가게 정보 관리 (영업시간·메뉴·소개) | ❌ | ✅ |
| **가게 공식 답변** (리뷰당 1회) | ❌ | ✅ |
| **사장님 전용 대시보드** | ❌ | ✅ |
| 이의제기 접수 | 이메일 (SLA 7일) | 전용 폼 (SLA 24시간) |
| 리뷰 삭제 요청권 | ❌ | ❌ **판매하지 않음** |

### 🏪 가게 홍보 사진 — 플랜별 차등 ✅ 확정

사장님이 직접 등록하는 **홍보용 사진** (외관·내부·룸·메뉴·플레이팅). 손님 리뷰 사진과는 **완전히 별개**다.

| 플랜 | 가게 홍보 사진 |
|---|---|
| **무료 사장님** | **최대 6장** |
| **유료 사장님** | **최대 20장** |

**구성 가이드 (권장 배분)**

| | 무료 6장 | 유료 20장 |
|---|---|---|
| 외관 | 1 | 2 |
| 내부·좌석 | 2 | 5 |
| 룸·프라이빗 공간 | 1 | 3 |
| 대표 메뉴 | 2 | 8 |
| 기타(테이블 세팅·뷰 등) | 0 | 2 |

기념일 수요가 핵심인 서비스 특성상 **룸·좌석 사진의 유무가 곧 예약 결정**으로 이어진다. 6장으로는 룸 구조를 보여줄 수 없고, 20장이면 코스 전체와 공간을 다 보여줄 수 있다. 여기가 유료 전환 동기다.

### ⚠️ 홍보 사진과 리뷰 사진의 분리 원칙

이 서비스의 근간은 "광고 없는 진짜 후기"다. 사장님이 올린 홍보 사진이 손님 사진처럼 보이면 **서비스 전제가 무너진다.**

| 규칙 | 내용 |
|---|---|
| **시각적 구분 필수** | 홍보 사진에는 `가게 제공` 라벨 상시 표시 (호버·클릭 없이 보이게) |
| **영역 분리** | 상세 페이지에서 `가게 사진` 탭과 `손님 사진` 탭을 분리. 한 갤러리에 섞지 않는다 |
| **기본 노출 순서** | 대표 이미지 1장 외에는 **손님 사진 우선** 배치 |
| **검색·정렬 무영향** | 홍보 사진 장수는 노출 순위·평점에 어떤 영향도 주지 않는다 |
| **대시보드 제외** | 워드클라우드·별점 분포 등 통계 집계에 홍보 사진 미포함 |
| **훈장 무관** | 홍보 사진 수는 §14.5 훈장 산식에 반영하지 않는다 |
| **플랜 해지 시** | 6장 초과분은 **비공개 처리**(삭제 아님), 재구독 시 복구 |

### 콘텐츠 규칙

- 허용 포맷·검증 절차는 리뷰 사진과 **동일** (§6.2) — **GIF·동영상·음성 업로드 불가**
- 장당 최대 10MB, EXIF 제거, 재인코딩
- ⚠️ **타 매장·타 브랜드 이미지, 스톡 사진, 과도한 보정 금지** → 신고 사유
- ⚠️ 실제 제공하지 않는 메뉴·공간 사진 게시 금지 (표시광고법상 허위·과장 광고 소지)
- 손님이 올린 리뷰 사진을 사장님이 홍보 사진으로 **재사용 금지** (저작권·초상권)
- 신고 접수 시 리뷰와 동일하게 블라인드 → 관리자 검토

```sql
create table restaurant_images (
  id            uuid primary key default gen_random_uuid(),
  restaurant_id uuid not null references restaurants(id) on delete cascade,
  storage_key   text not null,
  category      text,              -- exterior | interior | room | menu | etc
  is_primary    boolean not null default false,
  sort_order    int not null default 0,
  visible       boolean not null default true,   -- 플랜 해지 시 초과분 false
  uploaded_by   uuid not null references profiles(id),
  created_at    timestamptz not null default now()
);

-- 플랜별 상한 검증
create or replace function max_restaurant_images(p_restaurant uuid) returns int as $$
  select case when exists (
    select 1 from restaurant_owners ro
    join profiles p on p.id = ro.user_id
    where ro.restaurant_id = p_restaurant
      and p.plan = 'owner_plus'
      and (p.plan_expires_at is null or p.plan_expires_at > now())
  ) then 20 else 6 end;
$$ language sql stable;
```

### 🏅 훈장 활용 키트 (핵심 상품)

**미쉐린 모델**: 별은 살 수 없지만, 가이드북과 명패는 사업이다.

| 구성 | 내용 |
|---|---|
| **실물 스티커** | 금·은·동 훈장 디자인, 매장 유리문 부착용 (연 1회 발송) |
| **아크릴 명패** | 획득 연월 각인, 카운터 비치용 |
| **PDF 인증서** | 즉시 다운로드·출력 |
| **웹 임베드 위젯** | 가게 홈페이지·예약 페이지에 삽입 (`<script>` 1줄, 실시간 평점 연동) |
| **SNS 카드 이미지** | 인스타·네이버 플레이스 게시용, 노랑 브랜드 톤 |
| **보도용 문구** | "2026년 7월 리얼 테이블 이달의 베스트 식당 선정" 공식 표현 |

⚠️ **키트는 훈장을 획득한 가게만 구매할 수 있다.** 미획득 가게가 훈장 디자인을 사용하면 소비자 기만이 되므로, 구매 자격을 `restaurant_medals` 보유 여부로 검증한다.
⚠️ 훈장 디자인·명칭은 **상표 등록** 후 사용, 무단 사용 시 법적 대응 근거 확보 (§19)

### 사장님 전용 대시보드

| 위젯 | 내용 |
|---|---|
| 별점 분포 | 도넛차트 + 전월 대비 변화 |
| 워드클라우드 | 우리 가게 리뷰 키워드 + **신규 등장 키워드 하이라이트** |
| 방문 목적 분포 | 기념일·데이트·모임 비율 |
| 월별 추이 | 리뷰 수·평점 12개월 라인 |
| 자치구 평균 대비 | 같은 구 동일 카테고리 평균과 비교 (⚠️ **경쟁 가게 개별 데이터는 미제공**) |
| 사진 갤러리 | 손님이 올린 사진 모아보기 (다운로드 ❌, 초상권·저작권) |

**이게 진짜 상품이다.** "지난달부터 '웨이팅'이 리뷰에 12번 등장했고 그 리뷰들의 평균 별점은 3.2다" — 사장님이 돈을 낼 이유는 리뷰를 지우는 권한이 아니라 이런 정보다.

### 이의제기 처리 (삭제권 아님)

⚠️ 유료 사장님은 **더 빨리 검토받을 뿐**이다. 삭제 여부는 §6.4 금지 콘텐츠 위반만으로 판정한다.

| | 판정 |
|---|---|
| 방문한 적 없는 허위 리뷰 | 삭제 검토 ✅ |
| 욕설·비방·개인정보 노출 | 삭제 ✅ |
| 경쟁업체 조직적 악평 | 삭제 + 제재 ✅ |
| **별점이 낮음** | **삭제 사유 아님** ❌ |
| **음식이 맛없다는 주관적 평가** | **삭제 사유 아님** ❌ |

- 처리 결과와 사유는 **리뷰 작성자에게도 통보**
- 월별 이의제기 접수·인용 건수를 **투명성 리포트로 공개** (§19)

---

### 참고 — "유료만 훈장/삭제권" 안을 채택하지 않은 근거

**(1) 법적 리스크**

<cite index="1-1">미국 FTC는 부정적 리뷰 삭제를 금지 행위로 규정하고 위반 시 최대 5만7144달러의 벌금을 부과하는 규정을 제정했으며, 한국 공정거래위원회 역시 대가성 리뷰 관련 지침 개정에 나섰다.</cite> <cite index="3-1">국내에서는 전자상거래법 제21조 제1항이 거짓·과장된 사실을 알리거나 기만적인 방법으로 소비자를 유인하는 행위를 금지하고 있다.</cite> <cite index="2-1">공정위는 표시광고법 위반을 이유로 리뷰를 조작한 업체에 1억4000만원의 과징금을 부과한 전례가 있다.</cite>

**(2) 훈장의 자기모순**

유료 가게만 훈장을 받으면, 훈장은 품질 신호가 아니라 결제 신호가 된다. 사용자가 이를 알아채는 순간 배지 가치가 0이 되고, **판매 상품 자체가 소멸**한다. 훈장을 무료로 열어야 훈장 키트가 팔린다.

**(3) 데이터 손실**

빌보드를 유료 가게로 한정하면 후보 모수가 급감해 §14.3 자격 요건(고유 리뷰어 5인 이상 등)을 충족하는 가게가 사라진다. 순위표가 3~4곳으로 쪼그라들면 콘텐츠로서 기능하지 못한다.

### 대안 (미채택, 기록용)

- **B. 사장님 과금 없음** — 신뢰 최상, 매출 최소
- **C. 명시적 광고 지면 분리** — 리뷰 목록과 물리적으로 분리된 `광고` 라벨 영역. 법적으로는 방어 가능하나 **제품 원칙 1과 정면 충돌**하므로 도입 시 원칙 개정 필요

## 22.4 결제 · 구독 처리 📋 Phase 2

| 항목 | 내용 |
|---|---|
| 결제 수단 | 국내 PG (토스페이먼츠 / 포트원 등) ❓ 확정 필요 |
| 과금 주기 | 월 구독 (연 결제 할인 검토) |
| 요금 | ❓ 미정 |
| 무료 체험 | ❓ 검토 |
| 해지 | 즉시 가능, 잔여 기간까지 이용, 자동갱신 해지 화면 2단계 이내 노출 |
| 환불 | 전자상거래법상 청약철회 기준 준수, 이용약관 명시 |
| ⚠️ 법적 요건 | **통신판매업 신고** 필요, 정기결제 사전 고지 의무 |

```sql
create table subscriptions (
  id          uuid primary key default gen_random_uuid(),
  user_id     uuid not null references profiles(id),
  plan        text not null,             -- user_plus | owner_plus
  status      text not null,             -- active | canceled | expired
  started_at  timestamptz not null default now(),
  expires_at  timestamptz not null,
  pg_order_id text,
  created_at  timestamptz not null default now()
);

create table restaurant_owners (
  id            uuid primary key default gen_random_uuid(),
  user_id       uuid not null references profiles(id),
  restaurant_id uuid not null references restaurants(id),
  biz_number    text not null,           -- 사업자등록번호
  verified_at   timestamptz,
  verified_by   uuid,
  unique (restaurant_id)
);
```

⚠️ 사장님 계정은 **사업자등록증 확인 후 관리자 승인**으로만 부여한다. 자동 승인 시 타인 가게를 선점하는 사고가 발생한다.

## 22.5 수익 모델 로드맵

| 단계 | 내용 |
|---|---|
| Phase 1 | 전 기능 무료. **결제 없음** — 리뷰 총량 확보가 최우선 |
| Phase 2 | 이용자 플랜(사진 10장) 출시 + 사장님 계정·사업자 인증 + 훈장 부여 시작 |
| Phase 3 | 사장님 대시보드·답변권 + **훈장 활용 키트** 정식 오픈 |

⚠️ **리뷰가 충분히 쌓이기 전에 과금하면 안 된다.** 데이터가 없는 상태의 유료 대시보드는 팔 물건이 아니다.

---

# 23. 다음 액션

1. **도메인·상표 확인** — 블로커 가능성이 있어 최우선
2. 로고 및 OG 이미지 제작
3. Tailwind 토큰 설정 (§3 컬러·타이포·모션 매핑)
4. **Phase 0 랜딩페이지 구현 및 배포**
5. 더미 JSON 데이터 생성 (식당 30곳 / 리뷰 120건, 키워드 사전 계산 포함)
6. Phase 0.5 프로토타입 착수
7. 훈장 디자인(금·은·동) 제작 및 **상표 출원**
8. 국세청 사업자등록 상태조회 API 신청
9. §21 미해결 중 **초기 데이터 확보 방식** 확정 후 Phase 1 진입
